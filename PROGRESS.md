# ความคืบหน้า TaroNex

> สรุปท้ายทุกหมุดหมาย เพื่อให้ session ถัดไปเริ่มได้เร็ว
> อัปเดตล่าสุด 20 ส.ค. 2569 (รอบสอง — วางผัง API)

---

## สถานะรวม

**ยังไม่เริ่ม M0** ตามแผนเดิม — แต่มี **ต้นแบบหน้าจอทั้ง 52 หน้าที่รันได้จริง**
สร้างไว้ก่อนเพื่อตรวจ UX ก่อนลงทุนทำฐานข้อมูล

| | |
|---|---|
| ใช้งานได้ที่ | https://taronex.theerawut.com/app |
| โค้ด | https://github.com/theispie/taronex-app (branch `main`) |
| บนเครื่อง | `/opt/taronex-app` · deploy key อยู่ที่ `/root/.ssh/taronex_deploy` |

---

## ทำเสร็จแล้ว

### หน้าจอครบ 52 หน้า (47 route) — frontend อย่างเดียว
Next.js 15 App Router · TypeScript strict · Tailwind · basePath `/app`
**ยังไม่มีฐานข้อมูล ไม่มี API** ข้อมูลทั้งหมดอยู่ใน `src/mock/data.ts`
รูปทรงข้อมูลตรงกับพจนานุกรมข้อมูล ตอนต่อ backend เปลี่ยนแค่ที่มาของข้อมูล

ดีไซน์ยกมาจาก `docs/screens/_shared.css` ของต้นแบบทั้งชุด ไม่มีสีที่คิดขึ้นเอง
ถ้อยคำไทยยกจากบล็อก “ถ้อยคำ (ห้ามแปลใหม่)” ของแต่ละ `NN.md`

### ผัง API ทั้งระบบ + หน้าสถานะภายใน (20 ส.ค. 2569)
**ยังไม่มี endpoint ที่ต่อฐานข้อมูล** รอบนี้วางโครงกับทะเบียน ไม่ได้ทำตรรกะธุรกิจ

| ไฟล์ | หน้าที่ |
|---|---|
| `src/lib/api/registry.ts` | ทะเบียน 97 endpoint — แหล่งความจริงเดียว ยกจาก `taronex-architecture.html` แล้วแก้ตามโมเดลคอลัมน์ใหม่ |
| `src/lib/api/errors.ts` | รหัสข้อผิดพลาด 20 ตัว + ตารางสถานะ HTTP + `ApiError` · `notFoundAcrossTenant()` |
| `src/lib/api/respond.ts` | `ok()` / `fail()` / `handler()` — **จุดที่จะใส่ `set_config('app.tenant_id', …, true)` ตอน M1** |
| `src/lib/api/audit.ts` | ตรวจทะเบียนกับกฎข้อ 4·5·6·7·10·11 อัตโนมัติ |
| `src/app/api/v1/meta/health` | ยิงได้จริง — เครื่อง หน่วยความจำ เวลารัน |
| `src/app/api/v1/meta/endpoints` | ยิงได้จริง — ทะเบียนทั้งชุด + ผลตรวจกฎ เป็น JSON |
| `src/app/internal/api` | หน้าภายใน — สถานะสด · ผลตรวจกฎ · ความคืบหน้าตามหมุดหมาย · ทะเบียนที่กรองได้ |

| `src/lib/api/screens.ts` | แผนที่หน้าจอ 52 หน้า ↔ endpoint ที่หน้านั้นเรียก · ตัวตรวจฟ้องสองทาง |
| `src/lib/api/openapi.ts` | แปลงทะเบียนเป็น OpenAPI 3.1 สด · ไม่ใช้ไลบรารี |
| `src/app/api/v1/meta/openapi` | ยิงได้จริง — สเปคสำหรับ Postman / Insomnia / Bruno / Swagger UI |

**สถานะตอนนี้** 101 เส้นทาง · ใช้ได้แล้ว 3 (meta) · ยังไม่ทำ 98 · **ผลตรวจผ่าน 9 เตือน 1**

**ไล่จากหน้าจอแล้วเจอของที่เอกสารสถาปัตยกรรมตกไป 3 จุด**
- `GET /invitations/:token` — หน้าจอ 44 ต้องอ่านคำเชิญก่อนรับ ถึงจะรู้ว่าอีเมลไม่ตรง
- `GET /tasks/:id/client-view` — หน้าจอ 39 ปุ่ม "ดูอย่างที่ลูกค้าเห็น" · ต้องใช้ serializer ตัวเดียวกับพอร์ทัลจริง
- `GET /plans` — หน้าจอ 36 ปุ่ม "ดูรายละเอียดแผน"

**เรื่อง Postman / Swagger — ตัดสินแล้ว** ไม่ติดตั้งอะไรลงแอป
ทะเบียนถูกแปลงเป็น OpenAPI 3.1 สดที่ `/api/v1/meta/openapi`
Postman นำเข้าด้วย URL นั้นได้ตรงๆ · Swagger UI ชี้ url มาที่เดียวกันได้
เหตุผล: เครื่อง 512 MB และสเปคที่เขียนแยกจากโค้ดจะไม่ตรงกันในที่สุด

**ตัวตรวจจับของจริงไปแล้วหนึ่งจุด** — `POST /workspaces/:id/leave` ไม่ได้ประกาศสิทธิ์ตามกฎข้อ 10
ทะเบียนขึ้นแดงทันที แก้แล้ว นี่คือเหตุผลที่มีชั้น audit ไม่ใช่แค่รายการ endpoint เฉยๆ

### สถานะที่ลูกค้าเห็น — ตัดสินแล้ว 20 ส.ค. 2569 (ปิดข้อค้าง 3 ขั้น/5 ขั้น)

**สถานะบนพอร์ทัลไม่คำนวณจากบอร์ดเด็ดขาด ทุกขั้นต้องมีคนกด ไม่มี auto ในทุกกรณี**

เหตุผล — ถ้าแปลงจากตำแหน่งคอลัมน์ ลูกค้าจะเห็นสถานะขยับทุกครั้งที่ทีมลากการ์ด
ลากผิดแล้วลากกลับ ลูกค้าก็เห็นกระพริบ และไม่มีใครตั้งใจบอกลูกค้าสักครั้ง
สำหรับสินค้าที่ขายงานประกันกับ SLA รับไม่ได้

ผลพลอยได้ — พอสถานะไม่ผูกกับบอร์ด ปัญหา 3 ขั้น/5 ขั้นก็หายไปเอง
ได้ 5 ขั้นตามต้นแบบ และ**ไม่ขัดกฎข้อ 8** เพราะไม่ได้ไปแตะคอลัมน์

5 ขั้น (ยกถ้อยคำจาก `screens/32.html`): รับเรื่องแล้ว → กำลังตรวจสอบ → กำลังแก้ไข → รอตรวจสอบผล → แก้ไขแล้ว
ค่าว่าง = ยังไม่มีใครรับเรื่อง ลูกค้าเห็น "ส่งเรื่องแล้ว รอเจ้าหน้าที่รับเรื่อง" (เป็นใบรับ ไม่ใช่การอ้างความคืบหน้า)

**ต้องเพิ่มลงพจนานุกรมข้อมูลตอน M1 — ยังไม่มีในเอกสาร**
```
tasks.portal_stage       portal_stage  NULL   ว่าง = ยังไม่มีใครรับเรื่อง
tasks.portal_stage_at    timestamptz   NULL
tasks.portal_stage_by    uuid FK       NULL   ใครเป็นคนกด
task_events.from_portal_stage / to_portal_stage       (task_events ไม่มี jsonb จึงต้องเพิ่มคอลัมน์)
```

**กติกา**
- `POST /tasks/:id/portal-stage` เป็น**ประตูเดียว** ที่สถานะเปลี่ยนได้ · `PATCH /tasks/:id` ต้องปฏิเสธ 400 (หลักเดียวกับกฎข้อ 4)
- ปุ่ม "รับเรื่อง" เรียกเส้นนี้ด้วย `stage=received` และรับเป็นเจ้าของถ้ายังว่าง
- ขั้นอื่น — **ใครก็ได้ที่มีสิทธิ์เขียนในโปรเจกต์** ไม่เกี่ยวกับตำแหน่งงาน (PM/BA/Dev ไม่ต่างกัน ตามกฎเดิม)
- ขั้น `resolved` — **PM เท่านั้น** เพราะเป็นคำสัญญากับลูกค้า ระดับเดียวกับการปิดการ์ด
- ทุกครั้งเขียน `task_events` (กฎข้อ 5) — ได้ประวัติว่าใครบอกลูกค้าอะไรเมื่อไร

**นาฬิกา SLA — ตัดสินแล้ว 20 ส.ค. 2569**
**เริ่มเดิน ณ วินาทีที่ลูกค้ากดส่ง** ไม่ใช่ตอนเจ้าหน้าที่กดรับเรื่อง
เวลาที่เรื่องนอนรออยู่จึงถูกนับ — ตรงกับสิ่งที่ลูกค้ารู้สึกจริง และทำให้เห็นว่ารับเรื่องช้าไปแค่ไหน
เดินตามเวลาทำการใน `tenants.business_hours` ตามที่ออกแบบไว้เดิม (กดส่งตี 2 วันเสาร์ นาฬิกาไม่วิ่งรวด)

ช่วงแรกของนาฬิกา = ลูกค้ากดส่ง → `portal_stage = received` **ช่วงนี้ยังไม่มีเจ้าของการ์ด**
`GET /sla/overview` ต้องดันเรื่องที่ยังไม่มีใครรับขึ้นก่อนเสมอ เพราะนาฬิกาเดินอยู่แต่ยังไม่มีใครถือ

**ข้อควรระวังที่ต้องตัดสินตอน M10 — ชนกับกฎข้อ 9 ของตัวเอง**
เจตนาที่ตั้งไว้คือใช้ประเมินการรับเรื่องของ PM ซึ่งทำได้ **แต่ต้องเลือกที่วาง**
- ✅ วางที่ **ศูนย์ SLA (หน้า 33)** เป็นตัวเลขราย*เรื่อง* — ตรงกับที่หน้านั้นทำอยู่แล้ว ("เรียงตามเวลาที่เหลือ")
- ❌ วางที่ **หน้ากิจกรรม (46–49)** เป็นตัวเลขราย*คน* — ผิดกฎข้อ 9 ตรงตัว
  `49.md` เขียนไว้เองว่า "ทันทีที่ PM เห็นตัวเลขที่คนอื่นไม่เห็น หน้านี้จะกลายเป็นเครื่องมือประเมินผล"
  และ `46.md` ว่า "ถ้ามีตัวเลขเรียงลำดับคนเมื่อไหร่ เดฟจะเริ่มลากการ์ดถี่ๆ ให้ดูขยัน"

ถ้าอยากได้รายงานประเมิน PM จริงๆ ควรเป็นรายงานราย**โปรเจกต์/ลูกค้า** แยกออกมา
(ซึ่งก็คือราย PM อยู่ดีเพราะ `projects.pm_user_id` มีคนเดียว) ไม่ใช่ยัดลงหน้ากิจกรรม

**ยังไม่ได้ลงไลบรารีใหม่เลย** — ทะเบียนกับตัวตรวจเขียนด้วย TypeScript ล้วน
Zod อยู่ในรายการที่อนุมัติแล้วแต่ยังไม่ลง เพราะเครื่องเหลือ RAM ว่างหลักสิบ MB

### เครื่องและการ deploy
- Droplet 512 MB + **swap 2 GB** (จำเป็น — ไม่มี swap แล้ว `pnpm install` และ `next build` ตายเพราะ OOM)
- `next build` บนเครื่องนี้ใช้เวลา ~20 วิ ผ่านได้ **ถ้ามี swap** (แก้สมมติฐานเดิมใน BUILD-PLAN ที่ว่าต้อง build ที่ GitHub Actions เท่านั้น)
- systemd: `taronex-web` (Next.js :3000) · `taronex-uploader` (หน้าอัปโหลดเอกสาร :8000)
- nginx: `/` หน้ารอ · `/app` ระบบ · `/prototype` ต้นแบบ 52 หน้า · `/readme` หน้าอัปโหลด
- HTTPS มาจาก Cloudflare (origin เสิร์ฟ HTTP:80)
- **`/readme` ปิดแล้ว 20 ส.ค. 2569** — nginx ตอบ 404 · `taronex-uploader` stop + disable
  **ไฟล์ยังอยู่ครบที่ `/opt/taronex/readme` ไม่ได้ลบ** · config เดิมสำรองไว้ที่
  `/etc/nginx/sites-available/taronex.bak-*` · เปิดคืนได้ด้วยการคืนบล็อกเดิมแล้ว start service
- `biome.json` เพิ่ม `files.includes` ไม่ให้สแกน `.next` — เดิมสแกนทั้ง 209 MB
  รันไม่จบใน 2 นาที ตอนนี้ 0.6 วิ

---

## การตัดสินใจที่เปลี่ยนจากสเปคเดิม — อ่านก่อนทำต่อ

### 1. แยก tenant ด้วย path ไม่ใช่ subdomain
`/app/<รหัสสุ่ม 12 ตัว>/…` แทน `{slug}.taronex.co`
รหัสอยู่ใน `src/lib/tenant-code.ts` พร้อมรายการคำสงวน เตรียมไว้สำหรับวันที่ให้ตั้งชื่อเอง

**ผลด้านความปลอดภัยที่ต้องจัดการตอนทำ backend:**
- รหัสใน URL **ไม่ใช่สิทธิ์** ต้องตรวจ membership ฝั่งเซิร์ฟเวอร์ทุก request
- พอร์ทัลลูกค้าอยู่ origin เดียวกับฝั่งทีมแล้ว → ขัดกฎข้อ 6 โดยตรง
  ต้องแยกด้วยโค้ด: คุกกี้คนละชื่อ คนละ secret · API พอร์ทัลต้องปฏิเสธ session ของทีม
  **ก่อนรับลูกค้าจริงควรกลับไปใช้คนละโดเมนตามสเปคเดิม**
- CSRF สำคัญขึ้นเพราะ origin เดียว → SameSite=Lax + ตรวจ Origin ทุก mutation
- ตั้ง `Referrer-Policy: same-origin` แล้ว (กันรหัสรั่วผ่าน Referer)
- `/readme` อยู่ origin เดียวกับระบบจริง มี upload + รหัสผ่านเดียว **ต้องย้ายออกหรือปิดก่อนขึ้นจริง**

### 2. ไม่มี `task_status` แล้ว — เหลือแต่คอลัมน์
กฎข้อ 8 ใน `CLAUDE.md` ถูกเขียนใหม่ทั้งข้อ อ่านหัวข้อ “โมเดลของบอร์ด” ก่อนแตะอะไรที่เกี่ยวกับการ์ด

เก็บจริงแค่ 2 ฟิลด์: `projects.board` (ชื่อ+ลำดับ) กับ `tasks.column_key`
กติกาคำนวณสดจากตำแหน่งและทิศทางการลาก

**ที่ต้องระวังตอนทำ `task_events`** คอลัมน์ลบได้แต่ประวัติลบไม่ได้ →
ต้องบันทึกชื่อคอลัมน์และตำแหน่ง ณ ตอนนั้นลงในเหตุการณ์ด้วย

### 3. เอกสารที่แก้ตามแล้ว / ที่ยังไม่ได้แก้
- ✅ `CLAUDE.md` (ทั้งใน repo และใน `readme/` สองสำเนา)
- ✅ `BUILD-PLAN.md` — กฎข้อ 8 · `guard_task_column` · เทส M5 เปลี่ยนเป็นเทสตามทิศทาง
- ❌ **`taronex-architecture.html` ยังนิยาม `task_status` เป็น enum อยู่** — ต้องแก้ตอนทำ M1

---

## ข้อขัดแย้งในสเปคที่พบตอนอ่าน (ยังไม่ได้แก้เอกสาร)

| เรื่อง | ขัดกันยังไง | ควรยึด |
|---|---|---|
| โดเมนพอร์ทัล | CLAUDE.md/BUILD-PLAN = `taronex-support.com` · SCREENS.md/06/30/32 = `support.digitalx.taronex.co` | `-support.com` (เหตุผลเรื่องคุกกี้) |
| บทบาท | 05.md/08.md = มีแค่ owner/member · ที่อื่น = 4 ค่า | 4 ค่า |
| `job_title` | 08.md = `users.job_title` · M1 + 43.md = `memberships` | `memberships` |
| Timeline export | 19.md = ฝั่ง server แปลง PNG/PDF · M7 = ยังไม่ทำ ใช้ `@media print` | M7 |
| กฎมีกี่ข้อ | architecture.html = 7 ข้อ · CLAUDE.md = 12 ข้อ | 12 ข้อ |
| ที่เก็บไฟล์ | enum `storage_provider = r2` · tech stack = DigitalOcean Spaces | Spaces |
| URL หน้าสมัคร | 01.md = `app.taronex.co/signup` · ที่อื่น = `taronex.co` | ไม่มี `app.` |
| รายการ stack | CLAUDE.md ไม่มี Sentry กับ react-email แต่ BUILD-PLAN มี | เติมเข้า CLAUDE.md |

---

## ทำต่อได้เลย — เลือกทางใดทางหนึ่ง

**ทาง 0 · ต่อจากรอบนี้ — เปิด endpoint จริงตัวแรก**
โครง API พร้อมแล้ว ขาดแค่ฐานข้อมูล ทำ M0 (docker + postgres) แล้วต่อ `GET /workspace`
เป็นตัวแรกเพื่อพิสูจน์ทางเดินทั้งเส้น: RLS → `set_config` → `resolveAccess` → serializer
พอเส้นแรกเดินได้ ที่เหลืออีก 94 เส้นทางคือการทำซ้ำ

**ทาง A · ทำ UX ให้กดได้จริงก่อน (ไม่ต้องรอ backend)**
กล่องเลือก/สลับมุมมองที่ยังกดไม่ได้ ~45 ปุ่ม · ลากวางบอร์ดด้วย dnd-kit
ได้ประโยชน์คือรู้ว่า UX ใช้ได้ไหมก่อนลงทุนทำฐานข้อมูล

**ทาง B · เริ่ม M0 ตามแผนเดิม**
Docker + Postgres 17 + MinIO + Mailpit · GitHub Actions build
(หมายเหตุ: swap ทำไว้แล้ว · ufw ยังไม่ได้เปิด · docker ยังไม่ได้ลง)

**ทาง C · เริ่ม M1 สคีมาและ RLS**
ต้องแก้ `taronex-architecture.html` ให้ตรงกับโมเดลคอลัมน์แบบใหม่ก่อน

---

## เดิม

## M0 · ตั้งฐานและเครื่อง — **เสร็จแล้ว 20 ส.ค. 2569**

**เครื่องขยับเป็น 1 GB / 25 GB** เพราะเข้าเกณฑ์ที่ BUILD-PLAN เขียนไว้เองครบทั้งสองข้อ
(dmesg มี OOM 6 ครั้ง · swap ใช้ 826 MB) หลังขยับ swap เหลือใช้ 12 MB

- [x] swap 2 GB + swappiness 10 (ติดถาวรผ่าน fstab)
- [x] ufw — เปิด 22/80/443 ปฏิเสธขาเข้าที่เหลือ
- [x] docker 29.7.2 + compose v5.5.0
- [x] Postgres 17.11 รันจริงแล้วผ่าน `docker-compose.dev.yml` (+ MinIO + Mailpit)
- [x] Vitest 4 + Playwright (chromium อย่างเดียว) · เทสต์ชุดแรก 39 ข้อ
- [x] GitHub Actions — tsc + biome + vitest · **เขียวครบทั้งสามขั้น**
- [x] Dockerfile · deploy/backup.sh · .env.example

**เรื่องความปลอดภัยที่เกือบพลาด** — Docker เขียนกฎ iptables เองและ**ข้าม ufw ได้**
ถ้า publish พอร์ตเป็น 0.0.0.0 Postgres จะโผล่ออกอินเทอร์เน็ตทั้งที่ ufw ปิดอยู่
`docker-compose.dev.yml` จึงผูกทุกพอร์ตกับ 127.0.0.1 เท่านั้น — ห้ามแก้

**เคลียร์ lint 62 จุดที่ค้างมาแต่เดิม** ไม่เคยโผล่เพราะ `biome check` เดิมสแกน `.next`
แล้วรันไม่จบ ตอนนี้ผ่านหมด และจัดรูปแบบทั้งโปรเจกต์ในคอมมิตแยกของตัวเอง

### สองข้อในแผนที่ตัดสินใหม่
- **ไม่ใช้ Caddy** — tenant แยกด้วย path แล้ว ไม่ต้องใช้ wildcard cert และ Cloudflare ทำ HTTPS ให้
  nginx ที่รันอยู่ทำงานได้ดี ประหยัด RAM 20 MB
- **แอปยังอยู่บน systemd ไม่ย้ายเข้า docker** — build บนเครื่องใช้ ~35 วิ และมี RAM พอแล้ว
  ไม่ต้องจ่าย DO Container Registry เพิ่ม $5/เดือน · Dockerfile เขียนไว้แล้วถ้าวันหนึ่งอยากย้าย

## M1 · สคีมาและ RLS — **เสร็จแล้ว 20 ส.ค. 2569**

**ฐานข้อมูลรันจริงแล้ว** · `/api/v1/meta/health` รายงาน `ok · PostgreSQL 17.11 · 28 ms` สด

| | |
|---|---|
| ตาราง | 24 · `src/db/schema.ts` |
| RLS policy | 21 · ทุกตารางที่มี `tenant_id` เปิดทั้ง ENABLE และ **FORCE** |
| trigger | 4 · `guard_task_column` · `guard_portal_stage` · `guard_last_owner` · `guard_column_exists` |
| เทสต์ | **60 ข้อผ่านหมด** |
| ข้อมูลตัวอย่าง | 2 ที่ทำงาน · 5 คน (หนึ่งคนอยู่ทั้งสองที่) · 4 ลูกค้า · 4 โปรเจกต์ · 40 การ์ด |

### จุดที่ต้องรู้ก่อนแตะฐานข้อมูล
- **`src/db/client.ts` เป็นทางเดียวที่เปิดธุรกรรมได้** · `withTenant()` ใส่
  `set_config('app.tenant_id', …, true)` ให้ — **`true` คือ LOCAL ห้ามเอาออก (กฎข้อ 3)**
- `withColumnMove()` และ `withPortalStageChange()` เป็นประตูที่ปลดล็อก trigger เฉพาะจุด
  ใช้ที่ `POST /tasks/:id/transition` และ `POST /tasks/:id/portal-stage` เท่านั้น
- role `app` เป็น `NOSUPERUSER NOBYPASSRLS` และไม่ใช่เจ้าของตาราง
  **ห้ามใส่ `postgres` superuser ลง `DATABASE_URL` เด็ดขาด** มันข้าม RLS ได้ทุกกรณีแม้ใส่ FORCE
- ค่าลับอยู่ที่ `/etc/taronex/web.env` สิทธิ์ 600
- `project_templates` ต่างจากตารางอื่น — `tenant_id` NULL = แม่แบบกลาง อ่านได้ทุกที่ทำงาน
  แต่ `WITH CHECK` ยังบังคับให้เขียนได้เฉพาะของตัวเอง

### สิ่งที่ค้นพบระหว่างทำ — บันทึกไว้กันลืม
**เทสต์ "A → B → A" ไม่จับ `LOCAL` ที่หายไป** เพราะทุกธุรกรรมตั้งค่าใหม่อยู่แล้ว
ตัวที่จับได้คือข้อที่ถาม **นอก**ธุรกรรม — พิสูจน์แล้วด้วยการลองเปลี่ยน `true` เป็น `false`
แล้วมีข้อตกพอดีหนึ่งข้อ **ห้ามลบเทสต์ข้อนั้น**

**drizzle ห่อข้อผิดพลาดของฐานข้อมูลเป็น "Failed query: …" แล้วเก็บของจริงไว้ที่ `.cause`**
ถ้าเทียบ `error.message` ตรงๆ regex จะไปตรงกับ *ข้อความ SQL* ทำให้เทสต์ผ่านทั้งที่ไม่ได้พิสูจน์อะไร
มีตัวช่วย `expectDbError()` ใน `rls.test.ts` ที่ไล่ตาม cause ให้สุดสาย — ใช้ตัวนั้นเสมอ

**เทสต์ในไฟล์นั้นแก้ข้อมูลจริง** จึงเรียก `seed()` ใน `beforeAll` ทุกรอบ
ไม่งั้นรอบสองจะทำงานบนข้อมูลที่รอบแรกแก้ค้างไว้แล้วตกแบบงงๆ

### ✅ แก้ `taronex-architecture.html` แล้ว
`task_status` · `column_labels` · `from_status`/`to_status` · `storage_provider = r2`
ถูกแทนด้วยโมเดลคอลัมน์ · `portal_stage` · Spaces ครบทุกจุด (สำรองไฟล์เดิมไว้ `.bak-*`)

## M2 · บัญชี หลายที่ทำงาน และบทบาท — **เสร็จแล้ว 23 ส.ค. 2569**

**24 endpoint ของ M2 ใช้ได้จริงครบ · หน้าจอ 01–05 · 42 · 43 · 44 ต่อ API แล้ว**
เกณฑ์ผ่านทั้งสี่ข้อตาม BUILD-PLAN ผ่านหมด (12 เทสต์ใน `accounts.test.ts`)

### ยืนยันด้วยการยิงจริงผ่าน https ครบวงจร
```
เจ้าของสมัคร → เพื่อนสมัครบัญชีตัวเอง → เจ้าของเชิญ → เพื่อนกดรับ
  จำนวนที่ทำงาน ก่อน 4 → หลัง 4      ← ไม่เกิดที่ทำงานใหม่
  เพื่อนเห็น 2 ที่ทำงาน (owner ที่ตัวเอง · member ที่ถูกเชิญ)
  อ่านคำเชิญโดยยังไม่ล็อกอินได้ (หน้าจอ 05)
  อีเมลไม่ตรงกดรับ → ปฏิเสธพร้อมบอกว่าส่งถึงใคร (หน้าจอ 44)
  ถอด owner คนสุดท้าย → E_LAST_OWNER · แต่งตั้งคนที่สองก่อนแล้วถอดได้
  เชิญคนในที่ทำงานที่ไม่ได้เป็น owner → 404
```

### แบบแผนเส้นทาง
endpoint ที่ผูกกับที่ทำงานอยู่ใต้ `/api/v1/t/{tenant}/…`
**รหัสใน path ไม่ใช่สิทธิ์** — `requireTenant()` ตรวจ `memberships` ทุก request · ไม่ผ่านตอบ 404

### บทเรียนสองข้อจากรอบนี้ — อย่าลืม
**1. เทสต์ระดับบริการไม่พอ ต้องยิงของจริงด้วย**
`requireTenant()` ถาม `memberships` ในขอบเขตที่ไม่ได้ตั้ง `app.user_id`
RLS จึงคืน 0 แถวเสมอ **เจ้าของที่ทำงานเองก็ได้ 404** เทสต์ 11 ข้อผ่านหมดเพราะเรียกฟังก์ชันตรงๆ

**2. ตัวตรวจในทะเบียนไม่ได้อ่านโค้ดจริง**
`/auth/me` เคยคืนรายการที่ทำงานมาด้วย ทำให้กลายเป็น endpoint ที่ **ห้า** ที่ข้าม tenant
ทั้งที่กฎข้อ 11 อนุญาตแค่สี่ ตัวตรวจจับไม่ได้เพราะดูจากธงในทะเบียน ไม่ได้ดูว่าโค้ดแตะอะไร

### สิ่งที่ต้องเพิ่มนอกพจนานุกรมข้อมูล
- `sessions.token_hash` — เอกสารเขียน `id` ว่า "เก็บเป็น hash ใน cookie" ซึ่งค้นย้อนกลับไม่ได้
- **โทเคนตั้งรหัสใหม่ไม่ได้ทำตาราง** ใช้ HMAC เซ็นด้วย hash รหัสปัจจุบันแทน
  ใช้ครั้งเดียวได้จริงเพราะพอตั้งรหัสใหม่ hash เปลี่ยน ลายเซ็นเดิมใช้ไม่ได้
  **ข้อเสียที่ยอมรับ** — เพิกถอนทีละใบก่อนหมดอายุไม่ได้
  ถ้าวันหนึ่งต้องการ ให้ทำตาราง `password_resets` แล้วย้ายมา

### ยังไม่ได้ทำใน M2
- **ยังไม่ได้ต่อ Resend** คำเชิญกับลิงก์ตั้งรหัสใหม่จึงยังไม่ส่งอีเมลจริง
  ใช้ `pnpm dev:invite <รหัสที่ทำงาน> <อีเมล>` ออกลิงก์เองระหว่างพัฒนา
- หน้าจอ 06–10 · 08ข (ตั้งค่าที่ทำงาน · รายชื่อสมาชิก · เชิญสมาชิก · โปรไฟล์)
  ยังแสดงข้อมูลจำลอง — API พร้อมแล้วทั้งหมด เหลือแค่ต่อ

## M3 · ลูกค้า โปรเจกต์ เฟส งานหลัก — **เสร็จแล้ว 23 ส.ค. 2569**

19 endpoint ใช้ได้จริง · หน้าจอ 11 · 12 · 13 · 14 · 15 · 16 · 28 · 29 ต่อ API แล้ว
**เกณฑ์ผ่านทั้งสองข้อ** (15 เทสต์) ยืนยันด้วยการยิงจริงผ่าน https

### กฎข้อ 10 — เพิ่มด่านระดับโปรเจกต์
`src/lib/api/project-access.ts` เป็นด่านเดียวของทุก route ที่แตะโปรเจกต์
ตรรกะยังอยู่ที่ `resolveAccess()` ตัวเดิม ไฟล์นี้แค่หาข้อมูลมาป้อน
ไม่มีสิทธิ์ = 404 · งานหลักไม่มีรหัสโปรเจกต์ใน URL จึงต้องหาโปรเจกต์แม่ก่อนตัดสิน

### สิ่งที่ต้องระวังตอนแก้ต่อ
**ลบงานหลักต้องเคลียร์ `feature_id` เองก่อนเสมอ** ไม่พึ่ง `ON DELETE` ของฐานข้อมูล
FK เป็น `NO ACTION` การลบตรงๆ จะถูกปฏิเสธ และถ้าใครเปลี่ยนเป็น `CASCADE`
วันหนึ่งการ์ดจะหายทั้งชุดโดยไม่มีใครทัน

**`[tenant]/layout.tsx` เป็นด่านตรวจสิทธิ์ของฝั่งหน้าเว็บ** เรียก `requireTenant()`
ไม่ได้เป็นสมาชิก = 404 · ยังไม่ล็อกอิน = ส่งไปหน้าเข้าสู่ระบบ

### หน้าจอที่ยังใช้ข้อมูลจำลอง — จะขึ้นผิดหรือ 404 กับที่ทำงานจริง
`[tenant]/page.tsx` (หน้าแรก · M8) · `board` `list` `timeline` `edit` `access` `files`
`tickets/*` (M4–M7) · `settings/roles` · `team` `search` `activity` `calendar` `notifications`
`my` (M8) · `templates/*` (M9) · `sla/*` (M10) · `portal/*` (M11)
**ทั้งหมดเป็นของหมุดหมายถัดไป ไม่ใช่ของค้าง**

## M4 · การ์ดและไฟล์แนบ — **API เสร็จแล้ว 23 ส.ค. 2569**

13 endpoint ใช้ได้จริง · 15 เทสต์ · **หน้าจอ 18 · 20 · 21 · 37 ยังไม่ได้ต่อ**

**เกณฑ์ผ่าน** `PATCH /tasks/:id` ที่มี `column_key` → **400 E_COLUMN_NOT_PATCHABLE**
ปฏิเสธทั้ง snake_case และ camelCase · ปฏิเสธก่อนแก้ฟิลด์อื่น ไม่ใช่แก้บางส่วนแล้วค่อยตก

### ที่ต้องแก้ฐานข้อมูลระหว่างทาง
`task_events.task_id` เดิมเป็น `NOT NULL` ทำให้**ลบการ์ดที่มีประวัติไม่ได้เลยสักใบ**
และตัด FK เองก็ไม่ได้เพราะ role `app` ถูก `REVOKE UPDATE` ตามกฎข้อ 5
เปลี่ยนเป็น nullable + `ON DELETE SET NULL` — ฐานข้อมูลตัดให้เอง
(referential action ไม่ติด REVOKE) แถวเหตุการณ์จึงยังอยู่เป็นหลักฐาน
พร้อมชื่อคอลัมน์ ณ ตอนนั้นที่เก็บไว้แล้ว

### ไฟล์แนบ — ไม่วิ่งผ่านเซิร์ฟเวอร์เลย
เบราว์เซอร์อัปโหลดตรงไปที่เก็บไฟล์ด้วยลิงก์ presign อายุ 5 นาที
เพราะเครื่องมี RAM 1 GB ถ้าไฟล์ 50 MB วิ่งผ่าน Next.js พร้อมกันสองสามคน
เครื่องจะถูก OOM killer ฆ่า
คีย์ขึ้นต้นด้วย `tenant_id` เสมอ — ย้ายหรือลบข้อมูลของที่ทำงานหนึ่งได้ทั้งก้อน
ชนิดไฟล์ใช้**รายการอนุญาต** ไม่ใช่รายการห้าม เพราะรายการห้ามมีของหลุดเสมอ
ยิงจริงครบวงจรแล้ว: presign → PUT ตรงไป MinIO → บันทึก → ดาวน์โหลด → ลบ

**เครื่องจริงต้องเปลี่ยน `S3_ENDPOINT` เป็น Spaces และ `S3_FORCE_PATH_STYLE=false`**
ตอนนี้ `/etc/taronex/web.env` ชี้ไป MinIO ในเครื่อง

## M5 · เครื่องยนต์เปลี่ยนคอลัมน์ ⭐ — **เสร็จแล้ว 23 ส.ค. 2569**

2 endpoint · 17 เทสต์ · `src/lib/transition.ts`
หน้าจอ M4+M5 (18 · 20 · 21 · 37) ต่อ API แล้ว

**เกณฑ์ผ่านครบ** เข้าคอลัมน์สุดท้าย PM เท่านั้น · ลากถอยหลังต้องมีเหตุผล ·
ตีกลับแล้วการ์ดกลับไปหาเจ้าของคนก่อน (อ่านจาก `task_events` ไม่ใช่เก็บเป็นคอลัมน์
เพราะการ์ดถูกตีกลับหลายรอบได้ และเจ้าของคนก่อนของแต่ละรอบไม่เหมือนกัน)

การตัดสินทิศทางใช้ `checkMove()` ใน `types.ts` **ตัวเดียวกับที่หน้าจอใช้**
ฝั่งเซิร์ฟเวอร์กับหน้าจอจึงไม่มีทางตัดสินต่างกัน

## 🔴 บั๊กใหญ่ที่ซ่อนมาตั้งแต่ deploy ครั้งแรก — แก้แล้ว

**เว็บเสิร์ฟมาโดยไม่มี CSS และไม่มี JavaScript เลย**

`output: 'standalone'` ไม่คัดลอก `.next/static` กับ `public` ให้ (ปกติให้ CDN เสิร์ฟ)
เราไม่มี CDN ทุกไฟล์ CSS/JS จึงคืน 404 — **แต่หน้า HTML ยังคืน 200**
การยิง curl ดูรหัสตอบกลับจึงไม่มีทางเห็น ต้องเปิดเบราว์เซอร์จริงเท่านั้น

แก้ด้วย `scripts/after-build.mjs` ที่ผูกกับ `pnpm build` แล้ว **ห้ามเอาออก**

### บทเรียน — เพิ่มเทสต์ผ่านเบราว์เซอร์แล้ว
`e2e/transition.spec.ts` เดินเส้นทางเดียวกับที่คนใช้จริงเดิน
สมัคร → สร้างโปรเจกต์ → สร้างการ์ด → ย้าย → ตีกลับ → ตรวจว่าปิดงานไม่ได้
รันครั้งแรกจับได้ **3 บั๊กที่เทสต์ 119 ข้อกับ curl จับไม่ได้เลย**
1. static 404 ทั้งเว็บ (ข้างบน)
2. `router.push(r.next)` — API คืน `/app/<slug>` แต่ `basePath` เติม `/app` ให้อีก
   กลายเป็น `/app/app/<slug>` · สมัครเสร็จแล้วเด้งไปหน้า 404
3. รหัสโปรเจกต์รับแต่ตัวอักษร — `E2E` กับ `B2B` สร้างไม่ได้

**เปิด e2e ใน CI แล้ว** รันทุก push

### หน้าจอที่ยังไม่ได้ต่อ
`board` (M6) · `timeline` (M7) · `edit` `access` (M12) · หน้าแรก `team` `search`
`activity` `calendar` `notifications` `my` (M8) · `templates` (M9) · `sla` (M10) · `portal` (M11)

## M6 · บอร์ด — **เสร็จแล้ว 23 ส.ค. 2569**

ไม่มี endpoint ใหม่ · ใช้ของ M4/M5 ทั้งหมด · เป็นงาน UI ล้วน
`@dnd-kit` 2.5 MB · หน้าจอ 17 และ 17ข

**เกณฑ์ผ่านทั้งสามข้อ — พิสูจน์ด้วยการลากจริงด้วยเมาส์ใน `e2e/board.spec.ts`**
- ลากไปข้างหน้า → เด้งกล่องเลือกคนรับต่อ · ค่าเริ่มต้น "ให้ PM หาคนตรวจ"
  เรียงคนที่ถือน้อยสุดขึ้นก่อน เพื่อไม่ให้งานไปกองที่คนเดิม
- ลากเข้าคอลัมน์สุดท้ายทั้งที่ไม่ใช่ PM → ถูกปฏิเสธ **และการ์ดเด้งกลับที่เดิม**
  (optimistic update แล้วย้อนกลับเมื่อเซิร์ฟเวอร์ปฏิเสธ)
- เปิดลิ้นชักเปลี่ยน URL (`?card=ACM-1`) · ปุ่มย้อนกลับของเบราว์เซอร์ปิดลิ้นชัก

### สิ่งที่ไม่ได้ทำตามสเปคเดิม และเหตุผล
สเปคเขียนว่า "ลาก todo→review เด้งกล่องเลือกผู้ตรวจ" ซึ่งผูกกับชื่อสถานะ
พอไม่มี `task_status` แล้ว กล่องจะโผล่**ทุกครั้งที่ลากไปข้างหน้า** ไม่ใช่เฉพาะคอลัมน์ที่ชื่อ review
เพราะถ้าดูชื่อคอลัมน์มาตัดสิน ก็เท่ากับเอาการตั้งค่ากลับเข้าคอลัมน์ทางอ้อม (ขัดกฎข้อ 8)

จัดตามงานหลัก (17ข) เป็นมุมมองเดียวกัน ข้อมูลชุดเดียวกัน ต่างแค่วิธีจัดกลุ่ม
ลากข้ามงานหลักไม่ได้ เพราะการย้ายงานหลักไม่ใช่ transition (ใช้ `PATCH /tasks/:id`)

## M7 · Timeline — **เสร็จแล้ว 23 ส.ค. 2569**

1 endpoint · หน้าจอ 19 · วาดเป็น SVG ในเบราว์เซอร์

**เกณฑ์ผ่าน — พิสูจน์จากไฟล์ PDF จริงใน `e2e/timeline.spec.ts`**
เทสต์สั่งพิมพ์จริงแล้วอ่านไฟล์ที่ได้ออกมาตรวจ ไม่ใช่แค่ดูว่าปุ่มพิมพ์มีอยู่
- ฟอนต์ไทยไม่เพี้ยน — `IBMPlexSansThai-Regular/SemiBold` **ฝังในไฟล์จริง**
- แท่งไม่ถูกตัดกลางหน้า — 1 หน้าพอดี (`break-inside:avoid`)
- A4 แนวนอน — MediaBox 842.88 × 595.92 pt

### ไม่ทำส่งออกฝั่งเซิร์ฟเวอร์ และจะไม่ทำจนกว่าจะขยับเครื่อง
`resvg` กิน RAM 200–400 MB ต่อครั้ง เครื่อง 1 GB รับไม่ไหวถ้ามีคนกดพร้อมกันสองคน
`@media print` ให้ผลเหมือนกันโดยใช้ RAM ของเซิร์ฟเวอร์**เป็นศูนย์**
และฟอนต์ไทยไม่มีทางเพี้ยน เพราะใช้ฟอนต์ของเครื่องคนพิมพ์เอง
(ฟอนต์ไทยยังใส่ไว้ใน Dockerfile ตามแผน เผื่อวันที่เปิดฟีเจอร์นี้)

### รายละเอียดที่ตัดสินระหว่างทาง
- **ขอบเขตที่วาดกว้างกว่ากรอบโปรเจกต์ได้** ถ้างานล้นออกไปหลังกำหนดส่ง
  ถ้าตัดที่กรอบโปรเจกต์ แท่งที่เลยกำหนดจะหายจากจอ — ซึ่งเป็นแท่งที่สำคัญที่สุด
- เส้นแบ่งเป็นราย**เดือน** ไม่ใช่รายสัปดาห์ เพราะโปรเจกต์ยาวหลายเดือนแล้วเส้นจะถี่จนอ่านไม่ออก
- ส่วนที่เสร็จแล้วในแท่งคิดจาก**จำนวนการ์ด** ไม่ใช่จากเวลาที่ผ่านไป
- ไม่มีเส้นเชื่อมความสัมพันธ์ระหว่างงาน — ตัดออกตามสเปค

## M8 · งานของฉัน ทีม และการค้นหา — **เสร็จแล้ว 23 ส.ค. 2569**

8 endpoint · 12 เทสต์ · หน้าจอ 22 · 23 · 24 · 25 · 26 · 27 · 35 ต่อ API แล้ว

### เกณฑ์ผ่านทั้งสองข้อ
**ค้นคำไทยที่ไม่มีเว้นวรรค** — ใช้ `ILIKE` ไม่ใช่ full-text search
ภาษาไทยไม่มีเว้นวรรคระหว่างคำ ตัวตัดคำของ Postgres จึงมองทั้งประโยคเป็นคำเดียว
ค้น "อนุมัติ" ใน "รอหัวหน้าอนุมัติก่อนส่ง" จะไม่เจอเลยถ้าใช้ full-text
ต้องลงพจนานุกรมไทยซึ่งเพิ่มของที่ต้องดูแลบนเครื่อง 1 GB
**ถ้าวันหนึ่งช้า ให้เพิ่ม `pg_trgm` + GIN index ก่อนคิดเรื่องพจนานุกรม**

**ย้อนหลังคืนเจ้าของ ณ เวลานั้นถูกต้อง** — ไม่ได้อ่านจาก `tasks.assignee_id`
ซึ่งเป็นค่าปัจจุบัน แต่ไล่จาก `task_events` หาว่า ณ สิ้นวันนั้นการ์ดอยู่ในมือใคร
ทำได้เพราะทุกการย้ายเขียนเหตุการณ์หนึ่งแถวพร้อม `to_user_id` เสมอ (M5)
**ถ้าเส้นทางไหนขยับการ์ดโดยไม่ผ่าน transition ตัวเลขหน้านี้จะผิดทันที**
— นี่คือเหตุผลที่กฎข้อ 4 ต้องเข้มขนาดนั้น

### กฎข้อ 9 บังคับด้วยเทสต์แล้ว
`teamNow()` คืนแค่ `holding` กับ `flags` — ภาระตอนนี้ ไม่ใช่ผลงานสะสม
มีเทสต์ที่ระบุชื่อฟิลด์ต้องห้ามไว้ตรงๆ (`completed` `velocity` `score` `throughput`)
ถ้าวันหนึ่งมีใครเติมเข้ามา เทสต์จะแดงทันที
ทุกคนเห็นหน้านี้เหมือนกันหมด ไม่มีตัวเลขลับสำหรับ PM

### รายละเอียดที่ตัดสินระหว่างทาง
- **งานที่ได้รับจัดกลุ่มจากอาการ ไม่ใช่จากค่า `priority`** — การ์ด critical
  ที่ยังไม่ถึงกำหนด ไม่ได้เร่งกว่าการ์ดที่เลยกำหนดไปแล้ว
- ปฏิทินแสดงเฉพาะการ์ดที่มีวันกำหนด — **ไม่เดาวันให้เอง**
- โหมดช่วงเวลาจำกัดไม่เกิน 92 วัน กัน query บานปลายบนเครื่อง 1 GB
- ค้นหา escape `%` `_` `\` ก่อนใส่ใน LIKE ไม่งั้นค้น "100%" จะคืนทุกใบ

### ยังไม่ได้ทำ
**ส่งอีเมลแจ้งเตือน 3 ชนิด (assigned · rejected · mentioned) ยังไม่ได้ต่อ Resend**
หน้าศูนย์แจ้งเตือนอ่านจากตาราง `notifications` ได้แล้ว แต่**ยังไม่มีอะไรเขียนลงตารางนั้น**
ต้องทำตอนต่อ Resend — จุดที่ต้องเขียนคือ `transition()` และ `addComment()`

## M9 · แม่แบบ — **เสร็จแล้ว 23 ส.ค. 2569**

6 endpoint · 13 เทสต์ · แม่แบบกลาง 8 ชุด · หน้าจอ 40 · 41 ต่อ API แล้ว

**เกณฑ์ผ่าน** ยิงจริงยืนยัน: สร้างจากแม่แบบ HR ได้ **6 งานหลัก 21 การ์ด**
วันที่แปลงจากวันสัมพัทธ์ถูกต้อง · เลขการ์ดใบถัดไปเป็น HR-22 ไม่ซ้ำ
· แก้แม่แบบแล้วโปรเจกต์เก่าไม่เปลี่ยน (คัดลอกค่าออกมา ไม่ได้อ้างอิงกลับไป)

### สิ่งที่ค้นพบระหว่างทาง
**เพิ่มตัวนับการใช้งานบนแม่แบบกลางไม่ได้ — RLS ปฏิเสธ และปฏิเสธถูกแล้ว**
แม่แบบกลาง (`tenant_id = NULL`) เป็นแถวที่ทุกที่ทำงานใช้ร่วมกัน
การเขียนทับจึงเป็น**การเขียนข้ามที่ทำงาน** ซึ่งถ้าเปิดให้ทำได้จะเกิดสองปัญหา
หนึ่ง — ที่ทำงานหนึ่งเดาได้ว่าที่ทำงานอื่นใช้แม่แบบไหนบ่อย
สอง — ทุกที่ทำงานเขียนแถวเดียวกัน กลายเป็นจุดแย่งล็อก
**นับเฉพาะแม่แบบของทีม** ถ้าวันหนึ่งอยากรู้สถิติของแม่แบบกลาง ให้เก็บแยกเป็นตารางของระบบ

### กติกาที่บังคับไว้
- แม่แบบกลางแก้และลบไม่ได้จากฝั่งแอป (403) · RLS กันไว้อีกชั้น
- ถอดโปรเจกต์เป็นแม่แบบ **ตัดชื่อคน วันจริง และไฟล์ออกให้เอง**
  เก็บวันเป็นระยะห่างจากวันเริ่ม — คนใช้ไม่ต้องนั่งลบทีละอันและไม่มีทางลืม
- แตกแม่แบบอยู่ในธุรกรรมเดียวกับการสร้างโปรเจกต์ · ถ้าแตกพลาด โปรเจกต์ต้องไม่ถูกสร้างค้าง
- **ตั้ง baseline ทันทีตอนแตกแม่แบบ** ไม่รอให้คนมากดทีหลัง
  ถ้ารอ จะไม่มีใครกด แล้วตัวเลข "ขอบเขตบานปลาย" จะใช้ไม่ได้ทั้งระบบ
- ป้ายประเภทงานสูงสุด 3 ค่า เพราะช่องในฐานข้อมูลมีแค่ a/b/c

## 🔴 build โดน OOM — แก้แล้ว
`next build` กิน heap เกินค่าเริ่มต้นของ Node (~475 MB) เมื่อโปรเจกต์โตถึง 167 ไฟล์
ตั้ง `NODE_OPTIONS=--max-old-space-size=768` ใน `pnpm build` แล้ว
**ถ้าวันหนึ่งไม่พออีก ให้ย้าย build ไป GitHub Actions ตามแผนเดิม** อย่าไล่เพิ่มตัวเลขไปเรื่อยๆ
เพราะเครื่องมี RAM 961 MB และ Postgres จองไว้ 200 MB

## ทำต่อ — เหลืออย่างเดียวคือของที่ต้องใช้คีย์จากภายนอก

**API ครบ 102/102 · หน้าจอต่อข้อมูลจริงครบ 49/49 · ไม่มีข้อมูลจำลองเหลือแล้ว**

| ต้องมี | สถานะ | ผลที่ยังทำไม่ได้ |
|---|---|---|
| ที่เก็บไฟล์ (แนะนำ **Cloudflare R2** — ฟรี 10 GB ไม่คิด egress) | ยังชี้ MinIO บนเครื่อง | อัปโหลดไฟล์ใช้ได้แค่บนเครื่องนี้ |
| **Resend API key** | ยังไม่มี | คำเชิญ · ลิงก์พอร์ทัล · ลิงก์ตั้งรหัสใหม่ ยังไม่ถูกส่งจริง (ลง log แทน) |

พอมี Resend แล้วค่อยเขียน `notifications` ที่ `transition()` กับ `addComment()` ทีเดียว
ตอนนี้หน้าศูนย์แจ้งเตือนอ่านได้แต่ยังว่างเปล่าเสมอ เพราะยังไม่มีอะไรเขียนลงตารางนั้น

## หน้ากลาง "ที่ทำงานของฉัน" (42) — รื้อใหม่ตามต้นแบบ

### 🔴 ปัญหาที่แท้จริงไม่ใช่เรื่องสวยงาม
เดิมเปิดหน้านี้ตอน**ยังไม่ได้ล็อกอิน** แล้วขึ้นกล่องแดง "ยังไม่ได้เข้าสู่ระบบ"
คู่กับปุ่ม "สร้างที่ทำงานใหม่" ที่กดแล้วก็ไม่สำเร็จ — อ่านเหมือนระบบพัง ทั้งที่แค่ยังไม่ได้ล็อกอิน
ตอนนี้ `E_UNAUTHENTICATED` พาไปหน้าเข้าสู่ระบบทันที มีเทสต์เบราว์เซอร์คุมไว้

(ตรวจแล้วว่าตอนล็อกอินอยู่ ชื่อบริษัทขึ้นปกติมาตลอด — API ไม่เคยพัง)

### หน้าตาตามต้นแบบ 42
แถบบนมีโลโก้กับอีเมล · ทักทายด้วยชื่อจริง · แถวละ 38px มีอักษรย่อสี ป้ายบทบาท
และตัวเลขประกอบ · ปุ่มล่างวางคู่กัน · กว้าง 620px ไม่ใช่กล่องแคบแบบหน้าเข้าสู่ระบบ

**ป้ายบทบาทติดทุกแถว** เพราะคนที่เข้าในฐานะแขกหรือผู้ชมต้องรู้*ก่อนกด*ว่าจะเห็นไม่ครบ
ไม่งั้นจะนึกว่าระบบพัง (สเปคหน้า 42 เขียนไว้ตรงๆ)

### ตัวเลขประกอบ — และเหตุผลที่ต้องนับทีละที่ทำงาน
`GET /me/workspaces` คืน `members` · `projects` · `waitingOnYou` เพิ่ม
สเปคอนุญาตไว้ว่า "ตัวเลขรอคุณเป็นสิ่งเดียวที่ข้ามที่ทำงานได้ เพราะเป็นการนับ
ไม่ใช่การเอาข้อมูลมาปน" (กฎข้อ 11)

เส้นนี้ทำงานใน `withAccount()` ซึ่งตั้งแค่ `app.user_id` **ไม่ได้ตั้ง `app.tenant_id`**
policy ของ `projects`/`tasks` ยึด tenant_id ถามในธุรกรรมนั้นจึงได้ 0 แถวเสมอ —
ไม่ใช่เพราะไม่มีข้อมูล แต่เพราะ RLS ปิดอยู่

**ทางลัดที่ผิดคือไปเพิ่ม policy ให้ตารางพวกนี้อ่านข้าม tenant ได้**
เท่ากับเจาะกำแพงหลักของระบบเพื่อโชว์ตัวเลขบนหน้าจอเดียว
จึงเปิดธุรกรรมแยกต่อหนึ่งที่ทำงานแทน · N คือจำนวนที่ทำงานที่คนคนนี้อยู่ (ปกติ 1–3)
มีเพดานกันไว้ที่ 12 · จำนวนโปรเจกต์ที่เห็นคำนวณด้วย `resolveAccess()` ตัวเดียวกับทั้งระบบ

### คำเชิญที่ค้างอยู่
แสดงได้ แต่**กดรับจากหน้านี้ไม่ได้** — ต้องใช้ลิงก์ในอีเมล เพราะลิงก์คือหลักฐานว่า
คำเชิญส่งถึงคนคนนี้จริง · `/me/invitations` จึงไม่คืนโทเคนออกมาโดยตั้งใจ
ข้อความบอกตรงๆ ว่ายังไม่ได้ต่อบริการส่งอีเมล ให้ขอลิงก์จากคนที่เชิญโดยตรงไปก่อน

## ปิด /internal และปิดพอร์ตที่เปิดโล่ง — 25 ส.ค. 2569

### `/app/internal` ต้องใส่รหัสผ่านแล้ว
ปิดที่ **nginx** ไม่ใช่ในแอป เพราะสองเหตุผล
1. แอปพังเมื่อไหร่ หน้านี้ก็ยังปิดอยู่ · ถ้าปิดในแอป แอปพัง = ประตูเปิด
2. คำขอที่ไม่มีรหัสถูกตัดตั้งแต่ nginx ไม่ต้องวิ่งเข้า Node เลย

ผู้ใช้ `taronex` · รหัสอยู่ที่ `/root/.internal-password` (สิทธิ์ 600)
ไฟล์รหัส `/etc/nginx/.htpasswd-internal` เข้ารหัสด้วย bcrypt

### 🔴 พอร์ต 80 เคยเปิดให้ทั้งอินเทอร์เน็ต
DNS วิ่งผ่าน Cloudflare อยู่แล้ว แต่ ufw เปิด 80/443 ให้ทุกที่
ใครรู้ IP เครื่อง (`157.245.63.72`) ก็ยิงตรงเข้ามาได้ — **ข้าม WAF ข้าม rate limit**
และเห็นเว็บเป็น HTTP ธรรมดา · ใน log เจอสแกนเนอร์ยิงตรงมาแล้ว 168 ครั้งจาก IP เดียว

ปิดแล้วด้วย `/usr/local/sbin/taronex-cf-firewall` — เปิด 80/443 ให้เฉพาะช่วง IP ของ Cloudflare
(22 ช่วง · v4 + v6) ตอนนี้เหลือแค่ SSH ที่เปิดให้ทุกที่
**ช่วง IP ของ Cloudflare เปลี่ยนได้ — ถ้าวันหนึ่งเว็บเข้าไม่ได้ ให้รันสคริปต์นี้ก่อนเป็นอย่างแรก**

### ⚠ ที่ยังเหลือ — ช่วง Cloudflare→เครื่องยังเป็น HTTP ธรรมดา
คุกกี้เซสชันตั้ง `Secure` เบราว์เซอร์จึงส่งเฉพาะบน HTTPS (เบราว์เซอร์→Cloudflare ปลอดภัย)
แต่ Cloudflare ส่งต่อมาที่เครื่องด้วย HTTP ธรรมดา **คุกกี้กับรหัส basic auth
จึงวิ่งผ่านอินเทอร์เน็ตแบบไม่เข้ารหัสในช่วงนั้น**

ปิดได้ด้วยการตั้ง Cloudflare SSL เป็น **Full (strict)** แล้วลง **Origin Certificate** ที่เครื่อง
ต้องทำที่แดชบอร์ด Cloudflare จึงยังไม่ได้ทำ · ตอนนี้ความเสี่ยงจำกัดอยู่แค่คนที่ดักได้
ระหว่างศูนย์ข้อมูลของ Cloudflare กับ DigitalOcean

### บั๊กในสคริปต์ที่เจอระหว่างทำ (บันทึกไว้กันพลาดซ้ำ)
- ไฟล์ `ips-v4` ของ Cloudflare **ไม่มีบรรทัดใหม่ปิดท้าย** · `cat v4 v6` ตรงๆ
  ทำให้บรรทัดสุดท้ายติดกับบรรทัดแรกของ v6 แล้ว ufw ตอบ "Bad source address" · แก้ด้วย `awk 1`
- `set -e` + `n=$(… | grep …)` — grep ที่ไม่เจออะไรคืนสถานะ 1 แล้วสคริปต์ตายกลางคัน
  **กฎ v6 จึงค้างอยู่โดยสคริปต์ไม่ฟ้องอะไรเลย** · แก้ด้วย `|| true`
- ลบกฎ ufw ต้องอ่านเลขใหม่ทุกครั้ง เพราะเลขเลื่อนหลังลบ

## ระบบเชื่อมฐานข้อมูลจริงแล้ว — พิสูจน์ครบวง
สมัครผ่านโดเมนจริง → สร้างลูกค้า → สร้างโปรเจกต์ → สร้างการ์ด → อ่านกลับผ่าน API
แล้ว `psql` เข้าไปดูในฐานเจอแถวเดียวกัน (`CHK-1`) · ไม่ใช่ข้อมูลจำลองสักจุดเดียว

## หน้าภายใน · ผังฐานข้อมูล — `/internal/db`

ตอบคำถาม "มีตารางอะไร ฟิลด์อะไร ชนิดไหน ชี้ไปไหน" ได้ในหน้าเดียว
24 ตาราง · 240 ฟิลด์ · ค้นหาได้ทั้งชื่อตารางและชื่อฟิลด์

### แสดงสองด้านคู่กันเสมอ
**ผังในโค้ด** อ่านจาก `src/db/schema.ts` ผ่าน Drizzle ตอนรัน = สิ่งที่ตั้งใจให้เป็น
**ฐานจริง** อ่านจาก `pg_catalog` = สิ่งที่เป็นอยู่ตอนนี้ (RLS · FORCE · policy · trigger · role)

สองอย่างนี้ไม่ตรงกันได้ — migration ยังไม่รัน · `db/rls.sql` ยังไม่ลง · มีคนแก้ที่ฐานตรงๆ
ถ้าแสดงด้านเดียว คนอ่านจะเห็นความตั้งใจแล้วนึกว่าของจริงเป็นแบบนั้น
ซึ่งเป็นวิธีที่ทำให้ RLS หลุดโดยไม่มีใครรู้ · หน้าจะขึ้นเตือนทันทีถ้าสองด้านไม่ตรงกัน

### ผังไม่มีทางเก่า
ชื่อ ชนิด ว่างได้ไหม กุญแจนอก ดัชนี — ดึงจากของจริงทั้งหมด ไม่ได้พิมพ์ซ้ำ
สิ่งเดียวที่พิมพ์มือคือ **"ทำไม"** (เช่น ทำไมไม่มี `task_status`, ทำไม `task_events` ลบไม่ได้)
และมีเทสต์ตรวจว่าทุกบันทึกชี้ไปที่ตาราง/คอลัมน์ที่มีอยู่จริง — เปลี่ยนชื่อแล้วลืมแก้ เทสต์พังทันที

### เรื่องที่เจอระหว่างทำ
`role app` อ่าน `drizzle.__drizzle_migrations` ไม่ได้ (**ถูกแล้ว** ตารางนั้นเป็นของเจ้าของฐาน)
แต่คำสั่งที่ล้มใน Postgres ทำให้**ทั้งธุรกรรมเป็นโมฆะ** ข้อมูลอื่นที่อ่านสำเร็จไปแล้วหายหมด
หน้ากลายเป็น "ต่อฐานข้อมูลไม่ได้" ทั้งหน้า ทั้งที่ต่อได้ปกติ
แยกคำสั่งนั้นไปอยู่ธุรกรรมของตัวเองแล้ว · หน้าบอกตรงๆ ว่าอ่านไม่ได้เพราะอะไร

## 🔴 หน้าสัญญาเคยบันทึกค่าผิดแบบเงียบๆ — แก้แล้ว

`save()` อ่านค่าฟอร์มจาก state ซึ่งถูก "ปิดตาย" ไว้ตอน render รอบนั้น
กดบันทึกเร็วกว่าที่ React จะ render รอบใหม่ → ยิงค่าก่อนหน้าออกไป
**บันทึกเป็นเวอร์ชันใหม่ด้วยตัวเลขเดิม แล้วขึ้นว่า "บันทึกแล้ว" ตามปกติ**

คนพิมพ์แล้วกดไม่เร็วขนาดนั้น เจอเพราะเทสต์เบราว์เซอร์ล้มประมาณหนึ่งในสามรอบ
แต่ผลของมันคือเขียนค่าผิดเงียบๆ แล้วบอกว่าสำเร็จ ซึ่งย้อนหาไม่เจอถ้าเกิดกับลูกค้าจริง
จึงปิดด้วยโครงสร้าง — เก็บฟอร์มไว้ใน ref คู่กับ state แล้วให้ปุ่มอ่านจาก ref เสมอ

เทสต์ตัวใหม่จงใจ `fill()` แล้ว `click()` ทันที และ**ตรวจที่ฝั่งเซิร์ฟเวอร์ด้วย**
ไม่ใช่เชื่อแค่สิ่งที่หน้าจอวาด · รัน 5 รอบติดผ่านหมด

หน้าอื่นที่อ่าน state ในตัวจัดการปุ่มยังมีอีกหลายหน้า **ยังไม่ได้ไล่แก้** เพราะ
ผลของมันไม่ได้ร้ายเท่า (ส่วนใหญ่เป็นการสร้างของใหม่ ไม่ใช่ทับค่าเดิมเงียบๆ)
ถ้าเจอเทสต์ล้มแบบนี้อีกที่ไหน ให้แก้ด้วยรูปแบบเดียวกัน

## 🔴 เทสต์เคยล้างฐานข้อมูลของเว็บจริงทุกครั้งที่รัน — แก้แล้ว

เทสต์ 11 ไฟล์สั่ง `TRUNCATE` ทุกตารางใน `beforeEach`
และทุกไฟล์เขียน fallback เป็น `…/taronex` ซึ่งเป็น**ฐานเดียวกับที่เว็บจริงใช้**
(`/etc/taronex/web.env` ก็ชี้ `postgres://app@127.0.0.1:5432/taronex`)

บนเครื่องนี้ `pnpm test` จึงล้างฐานของเว็บจริงทุกครั้ง **โดยไม่มีอะไรเตือนเลย**
ยังไม่มีลูกค้าจริงจึงไม่เสียหาย แต่ถ้าปล่อยไว้ วันแรกที่มีข้อมูลจริงจะหายทั้งหมด

แก้ด้วยการกันสองชั้นที่ `src/test/db.ts`
1. ค่าเริ่มต้นชี้ `taronex_test` ไม่ใช่ `taronex`
2. **ปฏิเสธทุกชื่อฐานที่ไม่ลงท้ายด้วย `_test`** ต่อให้ตั้ง env ผิดก็ไม่ยอมรัน
   ชั้นที่สองคือชั้นที่ป้องกันจริง · พิสูจน์แล้วโดยชี้เทสต์กลับไปที่ฐานจริงแล้วมันปฏิเสธ

`pnpm test` เรียก `pnpm db:test:setup` ให้เองก่อนเสมอ (สร้างฐาน + migration + RLS)

**ที่ยังเหลือ** — เทสต์เบราว์เซอร์ (`pnpm test:e2e`) ยิงผ่านเว็บจริง
จึงยัง**สร้างที่ทำงานทดสอบทิ้งไว้ในฐานจริง** (ตอนนี้มี 10 ที่ทำงานที่เป็นขยะจากเทสต์)
ไม่ได้ล้างอะไร แต่ก่อนรับลูกค้าจริงควรมีเครื่อง staging แยกให้ e2e ยิง

## 🔴 ช่องโหว่ที่เจอและแก้แล้ว — `SESSION_SECRET` ไม่ได้ตั้งบนเครื่องจริง

โค้ดเขียนเป็น `process.env.SESSION_SECRET ?? 'dev-only-secret-…'` กระจายอยู่สองไฟล์
เครื่องจริง**ไม่เคยตั้งค่านี้เลย** จึงเซ็นโทเคนด้วยค่าที่อยู่ในโค้ดซึ่ง**เปิดสาธารณะบน GitHub**
ใครก็ได้ปลอมคุกกี้เซสชันพอร์ทัลได้ถ้ารู้ uuid ของที่ทำงานกับผู้ติดต่อ

มันไม่พังตอนทดสอบ ไม่มีอะไรฟ้อง หน้าเว็บทำงานปกติทุกอย่าง —
**นั่นคือเหตุผลที่ค่าเริ่มต้นแบบเงียบๆ อันตรายกว่าการไม่มีค่าเริ่มต้นเลย**

แก้แล้วสามชั้น
- รวมมาที่ `src/lib/auth/secret.ts` ที่เดียว
- `NODE_ENV=production` แล้วไม่มีค่า (หรือเป็นค่า dev หรือสั้นกว่า 32 ตัว) → **โยนข้อผิดพลาดทันที**
  ยอมให้เปิดไม่ขึ้น ดีกว่าเปิดขึ้นแล้วปลอมเซสชันได้ · มีเทสต์คุม 5 ข้อ
- ตั้งค่าจริง 64 ตัวอักษรบนเครื่องแล้ว และแก้ `.env.example` ให้ตรงกับของจริง
  (เดิมเขียนถึง `PORTAL_SESSION_SECRET` ที่โค้ดไม่มีจริง — ลบทิ้ง
  การแยกคุกกี้สองฝั่งทำด้วยการเติม `portal:` นำหน้าตอนเซ็น ไม่ใช่ตัวแปรที่สองที่ลืมตั้งได้อีกค่า)

## ✅ ย้าย build ไป GitHub Actions แล้ว

`.github/workflows/release.yml` — push ขึ้น main แล้วสร้างแพ็กเกจ 17 MB ออกเป็น release
`deploy/pull-release.sh` — เครื่องเป็นฝ่ายดึงเอง **ไม่ต้องตั้ง secret เพิ่มสักตัว**
เพราะ repo เปิดสาธารณะ · GitHub ไม่ต้องรู้จักเครื่องนี้และไม่ต้องเก็บกุญแจ SSH ไว้

`systemd` ชี้ที่ `/opt/taronex-releases/current` (symlink) การสลับเวอร์ชันจึงเป็นการเปลี่ยน
symlink ครั้งเดียว ไม่มีช่วงที่ไฟล์ปนกันสองเวอร์ชัน · เก็บย้อนหลัง 3 ชุด

**ทดสอบครบสามทางแล้ว** — ติดตั้งสำเร็จ · ลายนิ้วมือไม่ตรงแล้วไม่ติดตั้ง ·
เวอร์ชันที่เปิดไม่ขึ้นแล้วสลับกลับเองและเว็บกลับมาปกติ

รายละเอียดทั้งหมดอยู่ที่ `deploy/README.md` รวมถึงข้อควรระวังเรื่อง migration
(ต้องแยกสองรอบ deploy ถ้าจะลบคอลัมน์ ไม่งั้นถอยกลับไม่ได้)

## หน้าจอสองหน้าสุดท้ายต่อข้อมูลจริงแล้ว
- **08ข บทบาทและการโอนสิทธิ์** — แต่งตั้ง/ถอดเจ้าของได้จริง
  เทสต์เบราว์เซอร์พิสูจน์**สองชั้น**: ปุ่มปิดตัวเองตอนเหลือเจ้าของคนเดียว
  และยิง API ตรงข้ามหน้าเว็บไปก็ยังถูก trigger ปฏิเสธ (กฎข้อ 12 บังคับที่ฐานข้อมูล)
- **09 เชิญสมาชิก** — โควตาที่นั่งมาจากของจริง บอกก่อนกด ไม่ใช่ให้ไปเจอตอนกดส่ง

`src/mock/data.ts` (626 บรรทัด) กับ `src/components/stub.tsx` **ไม่มีใครใช้แล้ว**
ยังไม่ลบเพราะไม่ได้อยู่ในขอบเขตที่สั่ง — ลบได้เมื่อไรก็ได้ ไม่มีอะไรอ้างถึง

## M12 · สิทธิ์รายโปรเจกต์และโควตา — **เสร็จแล้ว 24 ส.ค. 2569**

6 endpoint · หน้าจอ 12 · 14 · 36 · 45
**เทสต์ 223 ข้อ + เบราว์เซอร์ 7 ข้อ ผ่านหมด** · ผลตรวจทะเบียน 19 ข้อผ่าน

### 🔴 บั๊กที่เจอตอนยิง HTTP จริง — `tenants` เป็นตารางเดียวที่ไม่มี RLS
เขียน `select plan from tenants limit 1` โดยไม่มี `WHERE` ทั้งใน `createProject()`
และ `archiveProject()` → **ได้แผนของที่ทำงานอื่นมาใช้จริง** (แผนฟรีกลายเป็นเพดาน 10)

`tenants` ไม่มีคอลัมน์ `tenant_id` ให้ policy ยึด จึงไม่มี RLS มาช่วยเหมือนตารางอื่น
**ทุก query ที่แตะ `tenants` ต้องมี `WHERE id = ...` เสมอ** — ตรวจแล้วที่เหลือถูกหมด

เทสต์หน่วยจับไม่ได้เพราะฐานทดสอบมีที่ทำงานเดียว
เทสต์ตัวใหม่จึงสร้างที่ทำงานที่สอง **และต้องทดสอบจากที่ทำงานแถวหลัง** —
`limit(1)` ที่ไม่มี ORDER BY คืนแถวตามลำดับกายภาพ ถ้าทดสอบจากแถวแรกจะมองไม่เห็นบั๊กเลย
(เขียนครั้งแรกแบบนั้น ถอด `WHERE` ออกแล้วยังผ่าน — ต้องรื้อใหม่)
พิสูจน์ด้วยการถอด `WHERE` ออกแล้วเทสต์พังหนึ่งข้อพอดี

### กฎข้อ 7 · ปิดโปรเจกต์คืนโควตาโดยไม่ลบอะไรเลย
ปิดแล้วการ์ด ไฟล์ ประวัติ นาฬิกา SLA อยู่ครบ · เปิดคืนได้ตลอดถ้าโควตายังว่าง
เพิ่มการบังคับโควตาตอน**สร้าง**ด้วย ซึ่งเดิมไม่มีเลย — เปิดได้ไม่จำกัดทุกแผน

หน้าโควตาเสนอ "ปิดโปรเจกต์ที่จบแล้ว" เป็นทางเลือกแรกในระดับเดียวกับการอัปเกรด ไม่ซ่อน
และ**ไม่มีปุ่มลดแผนแบบกดเอง** เพราะการลดแผนตอนใช้เกินโควตาต้องมีคนอธิบายก่อน

### กฎข้อ 10 · หน้าเว็บห้ามคำนวณสิทธิ์เอง
เดิมหน้า 45 เรียก `resolveAccess()` ฝั่งเบราว์เซอร์ ซึ่ง*ดูเหมือน*ถูกกฎเพราะใช้ฟังก์ชันเดียวกัน
แต่ข้อมูลที่ป้อนเข้าไป (แถวยกเว้น · ใครเป็น PM) มาจากคนละที่กับที่ route ใช้จริง
วันหนึ่งจะไม่ตรงกันแล้วตารางจะโกหก — ย้ายไปให้เซิร์ฟเวอร์ตัดสินแล้วส่ง `effective` มาที่เดียว
มีเทสต์เทียบว่าค่าที่หน้าจอเห็นตรงกับที่ `loadProject()` ตัดสินให้ route จริง

### ตารางเดียว สองหน้าที่
`project_members` เป็นทั้งรายชื่อยกเว้นของสมาชิก และใบผ่านของแขก
ได้สองฟีเจอร์จากตารางเดียว และไม่มีกติกาสองชุดให้ขัดกันเอง

### แก้ตัวเลขแผนที่เคยอยู่สองที่
`PLANS` เคยอยู่ทั้งใน route ของ `/plans` และตัวนับตอนปิดโปรเจกต์ — ย้ายมารวมที่ `src/lib/plans.ts`

## M11ข · หน้ากิจกรรม — **เสร็จแล้ว 24 ส.ค. 2569**

2 endpoint · หน้าจอ 46 · 47 · 48 · 49 · **เทสต์รวม 206 ข้อ + เบราว์เซอร์ 6 ข้อ ผ่านหมด**

### ⭐ กฎข้อ 9 · การตัดสินใจที่สำคัญที่สุดของหมุดหมายนี้
**จำนวนดิบไม่เคยออกจากเซิร์ฟเวอร์เลย** ตัดเป็นระดับความเข้ม 0–3 ตั้งแต่ชั้นข้อมูล
ถ้าคืนจำนวนออกไปให้หน้าเว็บตัดเอง วันหนึ่งจะมีคนเปิด DevTools แล้วเห็นตัวเลข
หรือมีใครเขียนหน้าใหม่ที่เอาไปแสดงตรงๆ — **พอตัดที่ต้นทาง ตัวเลขนั้นไม่มีอยู่ให้ใช้ตั้งแต่แรก**

ขอบเขตของแต่ละระดับเป็น**ค่าคงที่** ไม่ใช่เปอร์เซ็นไทล์ของกลุ่ม
ถ้าตัดตามค่าสูงสุดของกลุ่ม ระดับของคนหนึ่งจะเปลี่ยนเมื่อคนอื่นทำงานมากขึ้น
ซึ่งเป็นการเทียบคนโดยตรง — ตรงข้ามกับเจตนาของกฎข้อ 9 · มีเทสต์คุมข้อนี้ไว้

เรียงแถวตาม**ชื่อ** ไม่ใช่ตามปริมาณ · เรียงตามปริมาณคือการจัดอันดับคน
เทสต์จงใจให้คนที่ทำเยอะกว่ามีชื่อเรียงทีหลัง ถ้าเผลอเรียงตามปริมาณเมื่อไหร่จะพังทันที

**ข้อจำกัดที่ยอมรับตรงๆ** — ระดับ 0–3 ก็ยังเรียงลำดับหยาบๆ ได้
แต่หยาบพอที่จะไม่มีใครเอาไปทำ KPI และตรงกับที่ต้นแบบตั้งใจไว้
("ความเข้ม 4 ระดับ ไม่มีตัวเลขกำกับ") ถ้าจะเข้มกว่านี้คือตัดหน้านี้ทิ้งทั้งหน้า

### คนที่ไม่มีความเคลื่อนไหวต้องมีแถว
ถ้าแสดงเฉพาะคนที่มีเหตุการณ์ หน้านี้จะกลายเป็น**รายชื่อคนขยัน** ซึ่งแย่กว่าเดิม
แถวของทุกคนขึ้นเสมอ พร้อม**รหัสการ์ดที่ถืออยู่ ไม่ใช่จำนวนที่ถือ**
เพราะ "ไม่มีความเคลื่อนไหว" ไม่เท่ากับ "ไม่ได้ทำอะไร" — อาจติดอยู่กับการ์ดใบเดียวทั้งสัปดาห์
รหัสเอาไปเปิดดูได้ว่าติดอะไรอยู่ · จำนวนเอาไปเรียงลำดับคนได้ จึงไม่ส่ง

### เทสต์เบราว์เซอร์อ่านสิ่งที่ตาเห็น ไม่ใช่สิ่งที่ API คืน
`e2e/activity.spec.ts` ทำงาน 13 ครั้งแล้วยืนยันว่า**ไม่มีเลขสองหลักบนตาราง**
เพราะกฎข้อ 9 เป็นกฎเรื่องสิ่งที่ตาเห็น API สะอาดแค่ไหนก็ไม่ช่วยถ้าหน้าเว็บไปนับเอง

### `POST /tasks/:id/progress` ลงเป็นบันทึกภายในเสมอ
ไม่มีพารามิเตอร์ให้ส่งออกฝั่งลูกค้า (กฎข้อ 6)
ถ้าอยากบอกลูกค้าต้องไปกด `portal-stage` ซึ่งเป็นคนละการกระทำและมีคนรับผิดชอบชัดว่าใครบอก

### ไม่มีตารางใหม่
ทุกอย่างอ่านจาก `task_events` + `comments` ที่บันทึกอยู่แล้ว ไม่มีใครต้องกรอกอะไรเพิ่ม

## M11 · พอร์ทัลลูกค้า — **เสร็จแล้ว 24 ส.ค. 2569**

8 endpoint · หน้าจอ 06 · 30 · 31 · 32 และส่วนที่เหลือของหน้า 39

| | |
|---|---|
| เทสต์ | **193 ข้อผ่านหมด** (พอร์ทัล 18 · เดิม 175) |
| เทสต์เบราว์เซอร์ | **5 ข้อผ่านหมด** (เพิ่ม `e2e/portal.spec.ts`) |

### กฎข้อ 6 · แยกฝั่งลูกค้ากับฝั่งทีมให้ขาด
สเปคเดิมให้พอร์ทัลอยู่คนละโดเมน เบราว์เซอร์จะบังคับแยกคุกกี้ให้เอง
พอย้ายมา origin เดียวกัน (แยกด้วย path) **การแยกต้องทำด้วยโค้ดล้วน** — ทำสามชั้น
- คุกกี้คนละชื่อ `tnx_portal` ไม่ใช่ `tnx_session`
- เซ็นด้วย `portal:` นำหน้า ลายเซ็นของอีกฝั่งจึงใช้ข้ามไม่ได้แม้แชร์ secret เดียวกัน
- **tenant ถูกเซ็นไว้ในคุกกี้ แล้วเทียบกับ slug ใน URL ทุกคำขอ**
  ไม่งั้นผู้ติดต่อของที่ทำงาน ก แก้ URL เป็นของ ข แล้วอ่านข้อมูลข้ามได้ทันที

ยิงจริงแล้วทั้งสี่ทาง — คุกกี้ทีมใช้กับพอร์ทัลไม่ได้ · คุกกี้พอร์ทัลใช้กับ API ทีมไม่ได้ ·
คุกกี้ของ ก ใช้กับ ข ได้ 404 · และ `e2e/portal.spec.ts` เปิดเบราว์เซอร์จริงสองใบพิสูจน์ซ้ำ
(curl ถือ jar คนละใบอยู่แล้ว จึงพิสูจน์การแยกคุกกี้ไม่ได้ — ต้องเป็นเบราว์เซอร์)

### serializer ตัวเดียว ไม่ใช่สองตัว
`GET /tasks/:id/client-view` เรียกฟังก์ชันเดียวกับที่พอร์ทัลจริงเรียก
ถ้าเขียนตัวที่สอง วันหนึ่งจะต่างกัน แล้วหน้า "ดูอย่างที่ลูกค้าเห็น" จะโกหก
ซึ่ง**อันตรายกว่าไม่มีหน้านั้นเลย** เพราะทีมจะเชื่อมันแล้วเผลอเขียนอะไรที่ลูกค้าไม่ควรเห็น
เทียบ byte ต่อ byte ในเทสต์แล้วว่าสองเส้นคืนของเหมือนกันเป๊ะ

ที่ออกไปฝั่งลูกค้ามีแค่ 10 ฟิลด์ · วันที่เป็น `YYYY-MM-DD` ล้วน ไม่มีชั่วโมงนาที
ไม่มี assignee ไม่มี priority ไม่มีตัวเลข SLA ไม่มีคอมเมนต์ภายใน
ผลคัดแยกออกไปเป็น**ข้อความ** ไม่ใช่ค่าดิบ (`billable` → "เป็นงานนอกขอบเขตเดิม…")

### ⭐ ไม่มี auto — พิสูจน์แล้วด้วยเทสต์ที่จะพังทันทีถ้าใครเผลอทำ
ย้ายการ์ดข้ามคอลัมน์ผ่านประตูจริง แล้วยืนยันว่า `portal_stage` **ไม่ขยับ**
ลูกค้ายังเห็น "ส่งเรื่องแล้ว รอเจ้าหน้าที่รับเรื่อง" จนกว่าจะมีคนกด
ผลพลอยได้ที่สำคัญ — สถานะพอร์ทัลหลุดจากบอร์ดโดยสิ้นเชิง
ทีมเปลี่ยนคอลัมน์กี่ครั้งก็ได้โดยไม่กระทบสิ่งที่ลูกค้าเห็น (กฎข้อ 8)

### เซสชันพอร์ทัลไม่เก็บลงตาราง — และข้อเสียที่ยอมรับ
`portal_tokens` ในพจนานุกรมคือ**ลิงก์ครั้งเดียว** (มี `used_at`) ไม่ใช่เซสชัน
จะเอามาใช้เป็นเซสชันก็ผิดความหมาย และ CLAUDE.md ห้ามเพิ่มตารางใหม่โดยไม่ถาม
จึงเซ็นเป็นคุกกี้แบบไม่เก็บสถานะ — **เพิกถอนทีละใบก่อนหมดอายุไม่ได้**
ถ้าต้องตัดสิทธิ์ทันทีให้ลบผู้ติดต่อ ซึ่งตัดได้จริงเพราะทุกคำขอตรวจว่าผู้ติดต่อยังอยู่

### `pnpm dev:portal-link` — จงใจไม่ทำเป็น endpoint
โทเคนเข้าพอร์ทัล**ไม่มีทางออกทาง HTTP เลย** ต่อให้เป็นโหมด dev
ถ้ามีเส้นทางที่คืนโทเคนได้ วันหนึ่งจะมีคนเปิดมันบนเครื่องจริง
แล้วใครก็ได้จะเข้าพอร์ทัลของลูกค้าคนไหนก็ได้ · `e2e/portal.spec.ts` เรียกสคริปต์นี้เหมือนกัน

### จุดที่ต่างจากต้นแบบเล็กน้อย
เปลือกพอร์ทัลแสดงแค่ชื่อเอเจนซี่ (อ่านจาก slug ไม่ต้องล็อกอิน)
ส่วน**ชื่อผู้ติดต่อ**ย้ายไปอยู่หัวหน้าแรก ไม่ใช่มุมขวาบนของทุกหน้า
เพราะต้องมีเซสชันถึงจะรู้ว่าใคร และไม่คุ้มที่จะยิงคำขอเพิ่มอีกหนึ่งครั้งในทุกหน้าเพียงเพื่อแสดงชื่อ

## M10 · งานประกันและ SLA — **เสร็จแล้ว 23 ส.ค. 2569**

9 endpoint · หน้าจอ 33 · 34 · 38 · 39 (เฉพาะส่วนนาฬิกา)

| | |
|---|---|
| เทสต์ | **175 ข้อผ่านหมด** (business-hours 14 · sla 16 · เดิม 145) |
| ผลตรวจทะเบียน | 19 ข้อผ่าน |

### นาฬิกาเริ่มเดินตอนลูกค้ากดส่ง — ขัดกับต้นแบบ และเลือกตามการตัดสินใจ
`screens/38.md` เขียนว่า *"นาฬิกา SLA ยังไม่เดินจนกว่าจะคัดแยกเสร็จ ไม่งั้นเวลาจะถูกกินไปกับการตัดสินใจภายใน"*
แต่การตัดสินใจ 20 ส.ค. 2569 คือ **"นาฬิกาเดินตั้งแต่วินาทีที่ลูกค้ากดส่ง เพื่อประเมิน PM ในการรับเรื่อง"**
ยึดของหลัง และแก้ข้อความในหน้า 33/38 ให้ตรงกับพฤติกรรมจริงแล้ว

ข้อกังวลของต้นแบบยังถูกอยู่ จึงแก้ด้วยการ**แสดงแยก**แทนการซ่อน — `minutesBeforeTriage`
บอกว่าหมดไปกับการคัดแยกเท่าไร ขึ้นที่หน้า 39 เป็นกล่องแยกจากยอดรวม

### สามอย่างที่ทำให้ตัวเลขเถียงกับลูกค้าได้
- **ไม่มีคอลัมน์ยอดสะสม** — `usedMinutes` คำนวณสดจาก `sla_clock_events` ทุกครั้ง
  ถ้าเก็บยอดไว้ วันหนึ่งจะมีคนแก้ตัวเลขแล้วพิสูจน์ย้อนหลังไม่ได้
- **`sla_clocks` คัดลอกเวลาเป้าหมายมาเก็บตอนสร้าง** ไม่ join กลับไปที่นโยบายตอนอ่าน
  นี่คือกลไกเดียวที่ทำให้ "เปลี่ยนนโยบายแล้วเรื่องเก่าใช้ค่าเดิม" เป็นจริง
- **หยุดนาฬิกาต้องมีเหตุผลเสมอ** — บังคับที่ `pauseClock()` ไม่ใช่แค่ที่หน้าเว็บ

### ไม่มีงานเบื้องหลัง และไม่ต้องมี
เคยคิดจะทำ job `sla-tick` คอยขยับตัวเลข — **ไม่ต้อง** เพราะทุกอย่างคำนวณสดจากเหตุการณ์
สิ่งเดียวที่ต้องมี job จริงๆ คือ*การแจ้งเตือน*เมื่อใกล้ครบกำหนด ซึ่งติดที่ยังไม่มี Resend

### เวลาทำการ
`src/lib/business-hours.ts` · จ–ศ 09:00–18:00 ค่าเริ่มต้น · ตารางวันหยุดราชการไทยฝังไว้
เทสต์ที่สำคัญที่สุด: ศุกร์ 21 ส.ค. 15:00 + 8 ชม.ทำการ = **จันทร์ 24 ส.ค. 14:00**

### 🔴 เทสต์เบราว์เซอร์ไม่เคยมีเซสชันเลย — เพิ่งเจอ
`playwright.config.ts` ชี้ที่ `http://127.0.0.1:3000` มาตลอด
คุกกี้เซสชันตั้ง `Secure` เมื่อ `NODE_ENV=production` (ถูกแล้ว ของจริงอยู่หลัง HTTPS)
แต่ **Chrome ไม่ยอมเก็บคุกกี้ Secure จาก `http://127.0.0.1` ทั้งที่ยอมจาก `http://localhost`**
ทุกการเรียก API ในเทสต์จึงได้ 401 มาตลอด — รวมถึงบน GitHub Actions

แก้ด้วยการเปลี่ยน baseURL เป็น `localhost` ทั้งใน config และ CI · **ห้ามเปลี่ยนกลับ**
ทางแก้ที่ผิดคือเติมสวิตช์ปิด `Secure` ตอนเทสต์ — เท่ากับเปิดช่องให้เผลอใช้ผิดที่ในวันหลัง
ตอนนี้เทสต์เบราว์เซอร์ **4 ข้อผ่านหมด** (เพิ่ม `e2e/sla.spec.ts`)

### หน้า 39 ยังไม่ครบ — ตั้งใจ
แถบ `portal_stage` และ "ดูอย่างที่ลูกค้าเห็น" อยู่ใน M11
จงใจไม่เขียน serializer ชั่วคราวไว้ก่อน เพราะจะมีสองตัวที่ต้องตรงกันตลอดไป

## เดิม

## M0 · ตั้งฐานและเครื่อง — **เสร็จแล้ว 20 ส.ค. 2569**

**เครื่องขยับเป็น 1 GB / 25 GB** เพราะเข้าเกณฑ์ที่ BUILD-PLAN เขียนไว้เองครบทั้งสองข้อ
(dmesg มี OOM 6 ครั้ง · swap ใช้ 826 MB) หลังขยับ swap เหลือใช้ 12 MB

- [x] swap 2 GB + swappiness 10 (ติดถาวรผ่าน fstab)
- [x] ufw — เปิด 22/80/443 ปฏิเสธขาเข้าที่เหลือ
- [x] docker 29.7.2 + compose v5.5.0
- [x] Postgres 17.11 รันจริงแล้วผ่าน `docker-compose.dev.yml` (+ MinIO + Mailpit)
- [x] Vitest 4 + Playwright (chromium อย่างเดียว) · เทสต์ชุดแรก 39 ข้อ
- [x] GitHub Actions — tsc + biome + vitest · **เขียวครบทั้งสามขั้น**
- [x] Dockerfile · deploy/backup.sh · .env.example

**เรื่องความปลอดภัยที่เกือบพลาด** — Docker เขียนกฎ iptables เองและ**ข้าม ufw ได้**
ถ้า publish พอร์ตเป็น 0.0.0.0 Postgres จะโผล่ออกอินเทอร์เน็ตทั้งที่ ufw ปิดอยู่
`docker-compose.dev.yml` จึงผูกทุกพอร์ตกับ 127.0.0.1 เท่านั้น — ห้ามแก้

**เคลียร์ lint 62 จุดที่ค้างมาแต่เดิม** ไม่เคยโผล่เพราะ `biome check` เดิมสแกน `.next`
แล้วรันไม่จบ ตอนนี้ผ่านหมด และจัดรูปแบบทั้งโปรเจกต์ในคอมมิตแยกของตัวเอง

### สองข้อในแผนที่ตัดสินใหม่
- **ไม่ใช้ Caddy** — tenant แยกด้วย path แล้ว ไม่ต้องใช้ wildcard cert และ Cloudflare ทำ HTTPS ให้
  nginx ที่รันอยู่ทำงานได้ดี ประหยัด RAM 20 MB
- **แอปยังอยู่บน systemd ไม่ย้ายเข้า docker** — build บนเครื่องใช้ ~35 วิ และมี RAM พอแล้ว
  ไม่ต้องจ่าย DO Container Registry เพิ่ม $5/เดือน · Dockerfile เขียนไว้แล้วถ้าวันหนึ่งอยากย้าย

## M1 · สคีมาและ RLS — **เสร็จแล้ว 20 ส.ค. 2569**

**ฐานข้อมูลรันจริงแล้ว** · `/api/v1/meta/health` รายงาน `ok · PostgreSQL 17.11 · 28 ms` สด

| | |
|---|---|
| ตาราง | 24 · `src/db/schema.ts` |
| RLS policy | 21 · ทุกตารางที่มี `tenant_id` เปิดทั้ง ENABLE และ **FORCE** |
| trigger | 4 · `guard_task_column` · `guard_portal_stage` · `guard_last_owner` · `guard_column_exists` |
| เทสต์ | **60 ข้อผ่านหมด** |
| ข้อมูลตัวอย่าง | 2 ที่ทำงาน · 5 คน (หนึ่งคนอยู่ทั้งสองที่) · 4 ลูกค้า · 4 โปรเจกต์ · 40 การ์ด |

### จุดที่ต้องรู้ก่อนแตะฐานข้อมูล
- **`src/db/client.ts` เป็นทางเดียวที่เปิดธุรกรรมได้** · `withTenant()` ใส่
  `set_config('app.tenant_id', …, true)` ให้ — **`true` คือ LOCAL ห้ามเอาออก (กฎข้อ 3)**
- `withColumnMove()` และ `withPortalStageChange()` เป็นประตูที่ปลดล็อก trigger เฉพาะจุด
  ใช้ที่ `POST /tasks/:id/transition` และ `POST /tasks/:id/portal-stage` เท่านั้น
- role `app` เป็น `NOSUPERUSER NOBYPASSRLS` และไม่ใช่เจ้าของตาราง
  **ห้ามใส่ `postgres` superuser ลง `DATABASE_URL` เด็ดขาด** มันข้าม RLS ได้ทุกกรณีแม้ใส่ FORCE
- ค่าลับอยู่ที่ `/etc/taronex/web.env` สิทธิ์ 600
- `project_templates` ต่างจากตารางอื่น — `tenant_id` NULL = แม่แบบกลาง อ่านได้ทุกที่ทำงาน
  แต่ `WITH CHECK` ยังบังคับให้เขียนได้เฉพาะของตัวเอง

### สิ่งที่ค้นพบระหว่างทำ — บันทึกไว้กันลืม
**เทสต์ "A → B → A" ไม่จับ `LOCAL` ที่หายไป** เพราะทุกธุรกรรมตั้งค่าใหม่อยู่แล้ว
ตัวที่จับได้คือข้อที่ถาม **นอก**ธุรกรรม — พิสูจน์แล้วด้วยการลองเปลี่ยน `true` เป็น `false`
แล้วมีข้อตกพอดีหนึ่งข้อ **ห้ามลบเทสต์ข้อนั้น**

**drizzle ห่อข้อผิดพลาดของฐานข้อมูลเป็น "Failed query: …" แล้วเก็บของจริงไว้ที่ `.cause`**
ถ้าเทียบ `error.message` ตรงๆ regex จะไปตรงกับ *ข้อความ SQL* ทำให้เทสต์ผ่านทั้งที่ไม่ได้พิสูจน์อะไร
มีตัวช่วย `expectDbError()` ใน `rls.test.ts` ที่ไล่ตาม cause ให้สุดสาย — ใช้ตัวนั้นเสมอ

**เทสต์ในไฟล์นั้นแก้ข้อมูลจริง** จึงเรียก `seed()` ใน `beforeAll` ทุกรอบ
ไม่งั้นรอบสองจะทำงานบนข้อมูลที่รอบแรกแก้ค้างไว้แล้วตกแบบงงๆ

### ✅ แก้ `taronex-architecture.html` แล้ว
`task_status` · `column_labels` · `from_status`/`to_status` · `storage_provider = r2`
ถูกแทนด้วยโมเดลคอลัมน์ · `portal_stage` · Spaces ครบทุกจุด (สำรองไฟล์เดิมไว้ `.bak-*`)

## M2 · บัญชี หลายที่ทำงาน และบทบาท — **ทำไปครึ่งทาง**

**เกณฑ์ผ่านทั้งสี่ข้อตาม BUILD-PLAN ผ่านหมดแล้ว** (12 เทสต์ใน `accounts.test.ts`)
แต่ยังห่อเป็น route ไม่ครบ — ตรรกะอยู่ใน `src/lib/auth/accounts.ts` แล้วทั้งหมด

### ใช้ได้จริงแล้ว 11 เส้นทาง
```
POST /auth/signup · /auth/login · /auth/logout
GET  /auth/me · /me/workspaces
GET  /t/{tenant}/workspace · PATCH /t/{tenant}/workspace · GET /t/{tenant}/members
GET  /meta/health · /meta/endpoints · /meta/openapi
```
ยิงจริงผ่าน https ยืนยันแล้ว — ที่ทำงานตัวเอง 200 · **ของคนอื่น 404** · ไม่ล็อกอิน 401

### แบบแผนเส้นทาง
endpoint ที่ผูกกับที่ทำงานอยู่ใต้ `/api/v1/t/{tenant}/…` เพราะแยก tenant ด้วย path
**รหัสใน path ไม่ใช่สิทธิ์** — `requireTenant()` ตรวจ `memberships` ทุก request

### บั๊กที่เจอจากการยิงจริง ไม่ใช่จากเทสต์ — จำไว้
`requireTenant()` ถาม `memberships` ในขอบเขตที่ไม่ได้ตั้ง `app.user_id`
RLS จึงคืน 0 แถวเสมอ **เจ้าของที่ทำงานเองก็ถูกมองว่าไม่ใช่สมาชิก** ได้ 404
เทสต์ระดับบริการผ่านหมดเพราะเรียกฟังก์ชันตรงๆ ไม่ได้ผ่านเส้นทาง HTTP
**บทเรียน: ยิงของจริงเสมอ อย่าเชื่อเทสต์อย่างเดียว**

### เกือบผิดกฎข้อ 11 ด้วยความสะดวก
`/auth/me` เคยคืนรายการที่ทำงานมาด้วย ซึ่งทำให้มันกลายเป็น endpoint ที่ **ห้า**
ที่ query ข้าม tenant ได้ ทั้งที่กฎอนุญาตแค่สี่ — ตัดออกแล้ว
รายการที่ทำงานอยู่ที่ `GET /me/workspaces` ซึ่งอยู่ในสี่เส้นทางที่อนุญาต

### ที่ต้อง patch RLS เพิ่มตอนทำ M2
กฎข้อ 11 ชนกับ RLS โดยตรง — **ไม่เลือก**ทางให้แอปใช้ role ที่ `BYPASSRLS`
เพราะพอมีเครื่องมือที่ข้ามได้ วันหนึ่งจะมีคนใช้ผิดที่แล้วไม่มีใครรู้
เลือกเพิ่ม policy ที่แคบที่สุด — เห็นเฉพาะแถวที่เป็นของตัวเอง
`app.user_id` ตั้งเฉพาะใน `withAccount()` · ใน `withTenant()` เป็น NULL
ที่ทำงานหนึ่งจึงไม่เห็นว่าสมาชิกไปอยู่ที่ทำงานไหนอีกบ้าง (มีเทสต์ยืนยัน)

### เหลือใน M2 — ตรรกะพร้อมแล้ว เหลือห่อเป็น route
`/auth/forgot` · `/auth/reset` · `/auth/switch-tenant` · `/me/invitations`
`POST /workspaces` · `/workspaces/:id/leave` · `/members/invite` · `/members/:id`
grant-owner · revoke-owner · deactivate · `DELETE /members/:id` · `/account` · `/me`

**และหน้าจอ 01–10 · 42–44 ยังแสดงข้อมูลจำลอง ยังไม่ได้ต่อกับ API**
