# TaroNex

ระบบจัดการโปรเจกต์แบบ SaaS หลายผู้เช่า สำหรับ SME ไทย 5–50 คน
(ซอฟต์แวร์เฮาส์ · เอเจนซี่ · ครีเอทีฟ · การตลาด · บุคคล · อีเวนต์)

**ตัวแยกจากคู่แข่งคือ งานประกันหลังส่งมอบพร้อมนาฬิกา SLA และพอร์ทัลลูกค้าที่ไม่มีแบรนด์เรา**
ไม่ใช่บอร์ด Kanban — ถ้าต้องตัดอะไรเพื่อให้ทัน ตัดที่อื่นก่อน

---

## เอกสารในโปรเจกต์

| ไฟล์ | ใช้เมื่อไร |
|---|---|
| `docs/BUILD-PLAN.md` | เริ่มหมุดหมายใหม่ · หาว่าอะไรอยู่ในขอบเขต |
| `docs/taronex-architecture.html` | ทำฐานข้อมูลหรือ API · ดู ER · ดูกฎความปลอดภัย |
| `docs/SCREENS.md` | หาว่าหน้าจอไหนอยู่ไฟล์ไหน |
| `docs/screens/NN.html` | ต้นแบบล้วนของหน้าจอนั้น เปิดในเบราว์เซอร์ได้ |
| `docs/screens/NN.md` | เหตุผลการออกแบบ ถ้อยคำที่ต้องใช้ ข้อกำหนดทางเทคนิค |
| `docs/DESIGN-TOKENS.css` | ตัวแปรสีและฟอนต์ |

**ห้ามอ่าน `docs/taronex-prototype-v3.html` ทั้งไฟล์** — 296 KB ประมาณ 80,000 token
มันเป็นเครื่องมือดูต้นแบบสำหรับคน ไม่ใช่สำหรับอ่านเป็นข้อความ ใช้ `docs/screens/NN.html` แทน

ถ้าต้นแบบมีการแก้ ให้รัน `node scripts/extract-screens.mjs` เพื่อสร้างไฟล์แยกใหม่

---

## กฎที่ห้ามละเมิด

ตรวจสิบสองข้อนี้ก่อนส่งงานทุกครั้ง

1. **ทุกตารางที่มี `tenant_id` ต้องเปิด RLS + FORCE** แอปเชื่อมด้วย role `app` ที่เป็น `NOBYPASSRLS` และไม่ใช่เจ้าของตาราง
2. **ทุกธุรกรรมต้อง `set_config('app.tenant_id', …, true)` ก่อนคำสั่งแรก** ห้ามเขียน `WHERE tenant_id = ?` เองเป็นการป้องกันหลัก
3. **ห้ามใช้ `SET` โดยไม่มี `LOCAL` กับตัวแปร `app.*`** ค่าจะติดค้างกับ connection แล้ว request ถัดไปที่หยิบ connection เดิมจะเห็นข้อมูลของ tenant ก่อนหน้า — **นี่คือช่องโหว่ที่ร้ายแรงที่สุดที่เป็นไปได้ และจะไม่พังตอนทดสอบเพราะเครื่อง dev มี connection เดียว**
4. **`tasks.column_key` เปลี่ยนได้ทาง `POST /tasks/:id/transition` เท่านั้น** `PATCH /tasks/:id` ที่มี `column_key` ต้องตอบ 400
   นี่คือประตูเดียวที่การ์ดขยับได้ และเป็นเหตุผลที่เปลี่ยนกติกาของบอร์ดภายหลังได้โดยไม่ต้องรื้อทั้งระบบ
5. **`task_events` เขียนอย่างเดียว** REVOKE UPDATE, DELETE จาก role `app`
6. **พอร์ทัลใช้ serializer แยกโดยสิ้นเชิง** ห้ามส่ง `assignee` `priority` ค่า SLA เวลาที่มีชั่วโมงนาที หรือคอมเมนต์ภายในออกไป
7. **ไม่มี `DELETE` บนข้อมูลผู้ใช้ในเส้นทางที่เกี่ยวกับการเงิน** เกินโควตา ลดแผน ค้างชำระ ระงับบัญชี ล้วนปิดการเข้าถึงเท่านั้น
8. **คอลัมน์บนบอร์ดตั้งได้ 2–8 คอลัมน์ที่แม่แบบ และคอลัมน์มีแค่ชื่อกับลำดับ**
   ห้ามเพิ่มฟิลด์ตั้งค่าใดๆ ลงในคอลัมน์ — ไม่มีธง ไม่มี enum ไม่มี `is_done` ไม่มี `maps_to`
   กติกาทั้งหมด**คำนวณสดจากลำดับและทิศทางการลาก** ห้ามเก็บซ้ำเป็นคอลัมน์:
   - คอลัมน์แรก → การ์ดที่สร้างใหม่มาลงที่นี่
   - คอลัมน์สุดท้าย → ปิดงาน · PM เท่านั้นที่ย้ายมาได้ · นาฬิกา SLA หยุด · ไม่นับใน “ถืออยู่”
   - ลากถอยหลัง → ตีกลับ การ์ดกลับไปหาเจ้าของคนก่อน (อ่านจาก `task_events`)
     **ย้ายได้ทันที ไม่บังคับเหตุผล** — แก้เมื่อ 27 ส.ค. 2569 ตามที่เจ้าของสั่ง
     บอร์ดคือของที่คนขยับวันละหลายสิบครั้ง ถามทุกครั้งแล้วคนเลิกใช้ แล้วไปอัปเดตกันในแชทแทน
     ย้ายผิดแก้ง่าย ลากกลับก็จบ และทุกการย้ายยังลง `task_events` ครบเหมือนเดิม
   - ที่เหลือ → ย้ายเข้าออกได้ตามปกติ
9. **หน้ากิจกรรมห้ามมีตัวเลขที่เอามาเรียงลำดับคนได้** และห้ามมีตัวเลขที่ PM เห็นแต่คนอื่นไม่เห็น
10. **ตัดสินสิทธิ์ที่เดียว** ทุกที่เรียก `resolveAccess(user, project)` → `none | read | write` ห้ามมี route หรือคอมโพเนนต์ไหนตรวจสิทธิ์เอง
11. **มีสี่ endpoint เท่านั้นที่ query ข้าม tenant ได้** `/me/workspaces` · `/me/invitations` · `POST /workspaces` · `POST /invitations/:token/accept`
12. **ทุก tenant ต้องมี `role = owner` อย่างน้อยหนึ่งคนเสมอ** บังคับด้วย trigger ไม่ใช่แค่โค้ดฝั่งแอป

---

## โมเดลของบอร์ด — อ่านก่อนแตะอะไรที่เกี่ยวกับการ์ด

**ไม่มี `task_status` ในระบบนี้** ถูกตัดทิ้งโดยตั้งใจ ข้อมูลที่เก็บจริงมีแค่สองอย่าง

```
projects.board    jsonb   [{ key, name }, …]   ชื่อกับลำดับของคอลัมน์
tasks.column_key  text                          การ์ดใบนี้อยู่คอลัมน์ไหน
```

ทุกอย่างที่เหลือคำนวณสดจากตำแหน่ง — ปิดงานแล้วหรือยัง · สีบนหน้าจอ · ใครย้ายได้ ·
นาฬิกา SLA หยุดหรือยัง · ลูกค้าเห็นว่าถึงขั้นไหน

**ทำไมไม่ให้ตั้งค่าคอลัมน์** เพราะทันทีที่คอลัมน์มีฟิลด์ให้ตั้ง จะมีคนตั้งผิด
แล้วความผิดจะเงียบ — นาฬิกา SLA ไม่หยุด ลูกค้าเห็นสถานะผิด และไม่มีใครรู้จนกว่าจะมีคนทัก
ลำดับซ้ายไปขวาเป็นสิ่งที่คนใช้บอร์ดเข้าใจอยู่แล้ว ตั้งผิดได้ยากกว่ามาก

**สิ่งที่ต้องระวังตอนทำ `task_events`** คอลัมน์ลบได้และเปลี่ยนชื่อได้
แต่ `task_events` เขียนอย่างเดียว ลบไม่ได้ (กฎข้อ 5) จึงต้องบันทึกทั้ง `column_key`
และ **ชื่อคอลัมน์ ณ ตอนนั้น** กับ **ตำแหน่ง ณ ตอนนั้น** ลงในเหตุการณ์ด้วย
ไม่งั้นวันที่มีคนลบคอลัมน์ ประวัติย้อนหลังทั้งหมดจะชี้ไปที่ของที่ไม่มีแล้ว และซ่อมไม่ได้

**สิ่งที่ยังไม่ได้ตัดสิน** — พอร์ทัลลูกค้าตอนนี้แปลงเป็น 3 ขั้น (รับเรื่องแล้ว →
กำลังดำเนินการ → แก้ไขแล้ว) ตามคอลัมน์แรก/กลาง/สุดท้าย ถ้าอยากได้ 5 ขั้นเหมือนต้นแบบเดิม
ต้องมีอะไรบอกระบบเพิ่ม ซึ่งขัดกับกฎข้อ 8 — ให้ถามก่อนทำ

---

## ข้อจำกัดที่ต้องจำตลอด

**เครื่องจริงคือ DigitalOcean Droplet 512 MB** — Postgres 200 MB · Next.js 200 MB · Caddy 20 MB · ระบบ 80 MB เต็มพอดี

- ก่อนเพิ่มไลบรารีใดๆ **ให้ถามก่อน** พร้อมบอกขนาดและการใช้ RAM ตอนรัน
- `next build` ทำที่ GitHub Actions ไม่ใช่บนเครื่อง (ต้องการ 2 GB)
- ยังไม่ทำส่งออก Timeline เป็นไฟล์ฝั่งเซิร์ฟเวอร์ (`resvg` กิน RAM 200–400 MB ต่อครั้ง) ใช้ `@media print` แทน
- ห้ามเพิ่ม Redis · ไลบรารี Gantt สำเร็จรูป · monitoring stack

---

## เทคโนโลยี

TypeScript strict · Next.js App Router · PostgreSQL 17 · Drizzle ORM · Zod · TanStack Query
Tailwind + Radix primitives · dnd-kit · pg-boss · DigitalOcean Spaces (S3 SDK v3) · Resend
Caddy · Vitest · Playwright · Biome

**ห้ามเพิ่มนอกรายการนี้โดยไม่ถาม**

---

## โดเมน

```
taronex.co                        หน้ากลาง — เลือกที่ทำงาน · ตั้งค่าบัญชี
{slug}.taronex.co                 ฝั่งทีม
{slug}.taronex-support.com        พอร์ทัลลูกค้า  ← คนละโดเมนโดยตั้งใจ
```

พอร์ทัลอยู่คนละโดเมนเพราะสองเหตุผล — Let's Encrypt ออก wildcard ได้ชั้นเดียว
และคุกกี้ `.taronex.co` จะถูกส่งไปพอร์ทัลถ้าอยู่ใต้โดเมนเดียวกัน ซึ่งขัดกับกฎข้อ 6

---

## ข้อตกลงการทำงาน

**ทำทีละหมุดหมาย** อย่ากระโดดข้าม อย่าทำสองหมุดหมายใน session เดียว
จบหมุดหมายแล้วสรุปลง `docs/PROGRESS.md` แล้วเปิด session ใหม่

**ก่อนเขียนโค้ดทุกรอบ ให้สรุปสามบรรทัดก่อน**
1. จะแตะไฟล์ไหนบ้าง
2. กฎข้อไหนข้างบนที่เกี่ยวกับงานรอบนี้
3. มีอะไรในเอกสารที่ขัดกันหรือกำกวมไหม

**ห้ามสร้างคอลัมน์หรือตารางที่ไม่มีในพจนานุกรมข้อมูล**
ถ้าคิดว่าจำเป็น ให้หยุดแล้วอธิบายเหตุผล พร้อมเสนอทางเลือกที่ไม่ต้องเพิ่ม

**ถ้อยคำภาษาไทยใช้จากต้นแบบ อย่าแปลใหม่เอง**
"ถือมา 4 วัน" · "เกินมา 3 ชม." · "ไม่เกี่ยวกับเรา" · "งานเพิ่ม" · "ไม่มีความเคลื่อนไหว" ·
"รอคุณตัดสินใจ" · "ให้ PM หาคนตรวจ" — ทั้งหมดเลือกมาแล้วด้วยเหตุผล อยู่ในหัวข้อ "ถ้อยคำ" ของ `docs/screens/NN.md`

**ห้ามคิดสีใหม่เอง** ใช้ตัวแปรใน `docs/DESIGN-TOKENS.css` เท่านั้น

**ทุก PR ต้องมี** migration (ถ้าแตะสคีมา) · เทสต์ที่ครอบคลุมเส้นทางผิดพลาดไม่ใช่แค่เส้นทางสำเร็จ ·
ผ่าน `biome check` · ไม่มี `any` · ไม่มี `console.log` ค้าง

**ทุกฟีเจอร์ที่แตะข้อมูลข้ามผู้เช่าได้ ต้องมีเทสต์ที่พิสูจน์ว่าเข้าไม่ถึง**
สร้าง tenant A และ B แล้วยิงด้วย session ของ A ไปที่ข้อมูลของ B ต้องได้ **404 ไม่ใช่ 403**
(403 เป็นการยืนยันว่าข้อมูลนั้นมีอยู่จริง)

---

## คำสั่งที่ใช้บ่อย

```bash
pnpm dev              # เว็บ + Postgres + MinIO + Mailpit
pnpm test             # Vitest
pnpm test:e2e         # Playwright
pnpm db:generate      # สร้าง migration จาก schema.ts
pnpm db:migrate       # รัน migration (ใช้ DATABASE_MIGRATION_URL)
pnpm db:seed          # ข้อมูลตัวอย่าง 2 tenant
biome check --write .
node scripts/extract-screens.mjs   # สร้าง docs/screens ใหม่หลังแก้ต้นแบบ
```

---

## ศัพท์ในโดเมนนี้

| คำ | หมายถึง | ระวังสับสนกับ |
|---|---|---|
| **ที่ทำงาน** (tenant) | บริษัทหนึ่งที่เป็นลูกค้าของเรา | ลูกค้าของเขา (client) |
| **งานหลัก** (feature) | ก้อนงานที่ส่งมอบได้ 1–3 สัปดาห์ | เฟส |
| **เฟส** (phase) | วงจรชีวิตของโปรเจกต์ มีทีละ 1 ค่า | สถานะการ์ด |
| **คอลัมน์** (column) | การ์ดใบนี้ไปถึงไหน · แต่ละโปรเจกต์ตั้งเองได้ 2–8 คอลัมน์ | เฟส |
| **บทบาท** (role) | owner · member · viewer · guest — **สิทธิ์จริง** | ตำแหน่งงาน |
| **ตำแหน่งงาน** (job_title) | pm · dev · qa · design · ba — **แสดงผลเท่านั้น** | บทบาท |
| **PM** | `projects.pm_user_id` คนเดียวที่ปิดการ์ดได้ | ตำแหน่งงาน "PM" |
| **งานประกัน** (warranty) | `tasks.origin = warranty` หลังส่งมอบ | งานส่งมอบ (delivery) |

**จุดที่พลาดบ่อยที่สุด** — ตำแหน่งงานไม่เคยเปลี่ยนสิ่งที่ใครเห็นหรือทำได้ แม้แต่ครั้งเดียวในทั้งระบบ
